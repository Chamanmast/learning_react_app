{"slug":"node-express","title":"Node.js + Express - Complete Guide: Setup, Middleware & REST API","acc":"#e4572e","sidebar":"\n  <div class=\"brand\">\n    <h5><i class=\"bi bi-hdd-network me-2\" style=\"color:var(--node-green-bright)\"></i>Node + Express</h5>\n    <small class=\"d-block mt-1\">Node 22.x — REST API Guide</small>\n  </div>\n  <div class=\"py-2\">\n    <div class=\"nav-section\">Foundation</div>\n    <a class=\"nav-link\" href=\"#overview\"><i class=\"bi bi-braces\"></i> What is Node.js?</a>\n    <a class=\"nav-link\" href=\"#getting-started\"><i class=\"bi bi-download\"></i> Setup &amp; First App</a>\n    <a class=\"nav-link\" href=\"#modules\"><i class=\"bi bi-boxes\"></i> Modules &amp; Built-ins</a>\n\n    <div class=\"nav-section\">Express Core</div>\n    <a class=\"nav-link\" href=\"#express\"><i class=\"bi bi-cpu\"></i> Express Basics</a>\n    <a class=\"nav-link\" href=\"#middleware\"><i class=\"bi bi-diagram-3\"></i> Middleware Pipeline</a>\n    <a class=\"nav-link\" href=\"#routing\"><i class=\"bi bi-signpost-2\"></i> Routing</a>\n\n    <div class=\"nav-section\">Building an API</div>\n    <a class=\"nav-link\" href=\"#rest\"><i class=\"bi bi-broadcast\"></i> REST API Design</a>\n    <a class=\"nav-link\" href=\"#crud\"><i class=\"bi bi-list-check\"></i> Full CRUD Example</a>\n    <a class=\"nav-link\" href=\"#validation\"><i class=\"bi bi-check2-square\"></i> Validation</a>\n    <a class=\"nav-link\" href=\"#errors\"><i class=\"bi bi-exclamation-octagon\"></i> Error Handling</a>\n\n    <div class=\"nav-section\">Production</div>\n    <a class=\"nav-link\" href=\"#database\"><i class=\"bi bi-database\"></i> Database (MySQL)</a>\n    <a class=\"nav-link\" href=\"#auth\"><i class=\"bi bi-shield-lock\"></i> Auth &amp; JWT</a>\n    <a class=\"nav-link\" href=\"#testing\"><i class=\"bi bi-check-circle\"></i> Testing</a>\n    <a class=\"nav-link\" href=\"#deployment\"><i class=\"bi bi-rocket-takeoff\"></i> Deployment</a>\n\n    <div class=\"nav-section\">Reference</div>\n    <a class=\"nav-link\" href=\"#cheatsheet\"><i class=\"bi bi-stars\"></i> Cheat Sheet</a>\n  </div>\n","content":"\n\n  <!-- HERO -->\n  <div class=\"hero\">\n    <div class=\"row g-4\">\n      <div class=\"col-lg-6\">\n        <div class=\"hero-eyebrow\"><span class=\"dot\"></span> node -v &gt;= 22 — server runtime active</div>\n        <h1>Node.js <span class=\"box\">+ Express</span> + REST API</h1>\n        <p class=\"lede\">\n          The complete reference for building backend APIs with Node.js and Express —\n          event loop basics, the middleware pipeline, routing, JSON APIs, databases,\n          auth, and deployment. Written from a PHP/Laravel developer's point of view.\n        </p>\n        <div class=\"hero-badges\">\n          <span class=\"hero-badge\"><b>Node</b> 22 LTS</span>\n          <span class=\"hero-badge\"><b>Express</b> 5.x</span>\n          <span class=\"hero-badge green\"><b>ESM</b> native</span>\n          <span class=\"hero-badge green\"><b>npm</b> npx</span>\n        </div>\n        <div class=\"stat-strip\">\n          <div class=\"stat\"><span class=\"num\">1.5B+</span><span class=\"lbl\">npm installs / wk</span></div>\n          <div class=\"stat\"><span class=\"num\">50M+</span><span class=\"lbl\">weekly downloads</span></div>\n          <div class=\"stat\"><span class=\"num\">2Âµs</span><span class=\"lbl\">hello world / req</span></div>\n          <div class=\"stat\"><span class=\"num\">#1</span><span class=\"lbl\">API framework</span></div>\n        </div>\n      </div>\n      <div class=\"col-lg-6\">\n        <div class=\"pipe-panel\">\n          <div class=\"pipe-titlebar\">\n            <span class=\"traffic t1\"></span><span class=\"traffic t2\"></span><span class=\"traffic t3\"></span>\n            <span class=\"title\">request-lifecycle — bash</span>\n          </div>\n          <div class=\"pipe-body\">\n            <div class=\"pipe-route\">\n              <span class=\"method\">GET</span>\n              <span class=\"path\">/api/users</span>\n              <span class=\"arrow\">â†’</span>\n              <span class=\"path\">express pipeline</span>\n            </div>\n            <div class=\"pipe-stack\" id=\"pipeStack\">\n              <div class=\"pipe-node\" data-step=\"1\"><span class=\"idx\">01</span><span class=\"fn-name\">app.use(express.json())</span><span class=\"check\">âœ“</span></div>\n              <div class=\"pipe-node\" data-step=\"2\"><span class=\"idx\">02</span><span class=\"fn-name\">app.use(morgan('dev'))</span><span class=\"check\">âœ“</span></div>\n              <div class=\"pipe-node\" data-step=\"3\"><span class=\"idx\">03</span><span class=\"fn-name\">app.use('/api', userRouter)</span><span class=\"check\">âœ“</span></div>\n              <div class=\"pipe-node\" data-step=\"4\"><span class=\"idx\">04</span><span class=\"fn-name\">router.get('/users', handler)</span><span class=\"check\">âœ“</span></div>\n            </div>\n            <div class=\"pipe-console\" id=\"pipeConsole\">$ curl /api/users<span class=\"cursor\"></span></div>\n            <div class=\"pipe-actions\">\n              <button class=\"pipe-btn\" id=\"pipePlay\"><i class=\"bi bi-play-fill me-1\"></i>Send request</button>\n              <button class=\"pipe-btn ghost\" id=\"pipeReset\"><i class=\"bi bi-arrow-counterclockwise me-1\"></i>Reset</button>\n              <span class=\"pipe-hint\">requests flow top â†’ down through middleware</span>\n            </div>\n          </div>\n        </div>\n      </div>\n    </div>\n  </div>\n\n  <!-- ==================== OVERVIEW ==================== -->\n  <section id=\"overview\">\n    <h2><i class=\"bi bi-braces me-2\"></i>What is Node.js?</h2>\n    <p>\n      Node.js is a JavaScript <strong>runtime</strong> built on Chrome's V8 engine. Where PHP runs inside\n      a web server (Apache/nginx/FPM), Node runs its own process and speaks HTTP itself. The same\n      language that runs in the browser runs your backend — one language, two sides of the wire.\n    </p>\n\n    <div class=\"highlight-box\">\n      <strong>For Laravel/PHP developers:</strong> the mental model is closer to PHP-FPM + a framework\n      than to front-end JS. You write <code class=\"inline\">app.listen(3000)</code>, Express routes\n      replace <code class=\"inline\">routes/web.php</code>, middleware replaces your\n      <code class=\"inline\">$middleware</code> stack, and <code class=\"inline\">res.json()</code> replaces\n      <code class=\"inline\">response()-&gt;json()</code>.\n    </div>\n\n    <div class=\"card\">\n      <div class=\"card-header\">Node.js vs PHP vs React (one picture)</div>\n      <div class=\"card-body\">\n        <div class=\"table-responsive\">\n          <table class=\"table table-hover mb-0\">\n            <thead class=\"table-light\"><tr><th style=\"width:22%\"></th><th>Node.js</th><th>PHP (Laravel)</th><th>React (SPA)</th></tr></thead>\n            <tbody>\n              <tr><td><strong>Role</strong></td><td>Runtime + HTTP server</td><td>Runtime via web server</td><td>Client-side UI library</td></tr>\n              <tr><td><strong>Rendering</strong></td><td>Server (and client)</td><td>Server only</td><td>Client only</td></tr>\n              <tr><td><strong>Concurrency</strong></td><td>Single-thread event loop</td><td>Multithreaded (per request)</td><td>—</td></tr>\n              <tr><td><strong>Async style</strong></td><td>Callbacks â†’ Promises â†’ async/await</td><td>Synchronous (blocking)</td><td>Promises / async</td></tr>\n              <tr><td><strong>Data format</strong></td><td>JSON everywhere</td><td>Arrays + JSON</td><td>JSON</td></tr>\n              <tr><td><strong>Best for</strong></td><td>APIs, realtime, streaming</td><td>Traditional MVC apps</td><td>Interactive UI</td></tr>\n            </tbody>\n          </table>\n        </div>\n      </div>\n    </div>\n\n    <h3>The Event Loop (why you must never block)</h3>\n    <p>\n      Node runs <strong>one thread</strong> for your JavaScript. Slow code on that thread pauses\n      <em>every</em> request. Heavy work (files, DB, network) is handed off to the OS and resumed\n      via the callback queue — that's why async/await is not optional, it's the whole model.\n    </p>\n    <div class=\"card\">\n      <div class=\"card-body\">\n        <pre><code><span class=\"cm\">// BLOCKING â€” sleeps the whole server (avoid)</span>\n<span class=\"kw\">import</span> { execSync } <span class=\"kw\">from</span> <span class=\"str\">'node:child_process'</span>;\n<span class=\"kw\">const</span> <span class=\"tp\">output</span> = execSync(<span class=\"str\">'grep -r secret /var/log'</span>); <span class=\"cm\">// blocks every request</span>\n\n<span class=\"cm\">// NON-BLOCKING â€” async/await yields back to the event loop</span>\n<span class=\"kw\">import</span> { exec } <span class=\"kw\">from</span> <span class=\"str\">'node:child_process'</span>;\n<span class=\"kw\">const</span> <span class=\"fn\">getLogs</span> = <span class=\"kw\">async</span> () =&gt; <span class=\"kw\">await</span> exec(<span class=\"str\">'grep -r secret /var/log'</span>);</code></pre>\n      </div>\n    </div>\n  </section>\n\n  <!-- ==================== GETTING STARTED ==================== -->\n  <section id=\"getting-started\">\n    <h2><i class=\"bi bi-download me-2\"></i>Setup &amp; First App</h2>\n    <p>Install Node (use a version manager so `node` is easy to upgrade), then scaffold a project.</p>\n\n    <h3>Install Node with nvm</h3>\n    <div class=\"card\">\n      <div class=\"card-body\">\n        <pre><code><span class=\"cm\"># Windows: install via winget (or nvm-windows)</span>\nwinget install OpenJS.NodeJS.LTS\nnvm install 22\nnvm use 22\n\n<span class=\"cm\"># macOS / Linux</span>\ncurl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.40.1/install.sh | bash\nnvm install 22 &amp;&amp; nvm alias default 22\n\n<span class=\"cm\"># Verify</span>\nnode -v     <span class=\"cm\"># v22.x</span>\nnpm -v      <span class=\"cm\"># 10.x</span></code></pre>\n      </div>\n    </div>\n\n    <h3>Scaffold a project</h3>\n    <div class=\"card\">\n      <div class=\"card-body\">\n        <pre><code>mkdir my-api &amp;&amp; cd my-api\nnpm init -y                      <span class=\"cm\"># creates package.json</span>\n\n<span class=\"cm\"># Express 5.x + dev tooling</span>\nnpm install express\nnpm install --save-dev nodemon\n\n<span class=\"cm\"># Add scripts to package.json</span>\n<span class=\"cm\">#   \"start\": \"node src/index.js\"</span>\n<span class=\"cm\">#   \"dev\":   \"nodemon src/index.js\"</span></code></pre>\n      </div>\n    </div>\n\n    <h3>Hello World</h3>\n    <div class=\"card\">\n      <div class=\"card-header\">src/index.js</div>\n      <div class=\"card-body\">\n        <pre><code><span class=\"kw\">import</span> express <span class=\"kw\">from</span> <span class=\"str\">'express'</span>;\n\n<span class=\"kw\">const</span> <span class=\"fn\">app</span> = express();\n<span class=\"kw\">const</span> <span class=\"tp\">PORT</span> = process.env.PORT ?? <span class=\"num\">3000</span>;\n\napp.get(<span class=\"str\">'/'</span>, (req, res) =&gt; {\n  res.send(<span class=\"str\">'Hello from Express 5'</span>);\n});\n\napp.listen(PORT, () =&gt; {\n  console.log(<span class=\"str\">`API running on http://localhost:</span><span class=\"tp\">${PORT}</span><span class=\"str\">`</span>);\n});</code></pre>\n      </div>\n    </div>\n\n    <div class=\"version-pin\">\n      $ npm run dev &nbsp;»&nbsp; API running on http://localhost:3000\n    </div>\n\n    <div class=\"card\">\n      <div class=\"card-header\">package.json Scripts &amp; Key Fields</div>\n      <div class=\"card-body\">\n        <div class=\"table-responsive\">\n          <table class=\"table table-hover mb-0 cmd-table\">\n            <thead class=\"table-light\"><tr><th style=\"width:36%\">Field / Command</th><th>What it does</th></tr></thead>\n            <tbody>\n              <tr><td>npm run dev</td><td>Start with nodemon (auto-restart on save)</td></tr>\n              <tr><td>npm start</td><td>Run for production (node src/index.js)</td></tr>\n              <tr><td>\"type\": \"module\"</td><td>Enable native ES modules (import/export)</td></tr>\n              <tr><td>npm install</td><td>Install all dependencies from lockfile</td></tr>\n              <tr><td>npm install -g</td><td>Install a CLI tool globally</td></tr>\n              <tr><td>npx</td><td>Run a CLI without installing it globally</td></tr>\n              <tr><td>npm outdated</td><td>List packages with newer versions</td></tr>\n              <tr><td>npm audit</td><td>Scan dependencies for known vulnerabilities</td></tr>\n            </tbody>\n          </table>\n        </div>\n      </div>\n    </div>\n  </section>\n\n  <!-- ==================== MODULES ==================== -->\n  <section id=\"modules\">\n    <h2><i class=\"bi bi-boxes me-2\"></i>Modules &amp; Built-ins</h2>\n    <p>\n      Every file is a module. Since Node 20+ ESM (<code class=\"inline\">import</code> / <code class=\"inline\">export</code>) is the\n      default — the old CommonJS (<code class=\"inline\">require</code>) style still works for legacy code.\n    </p>\n\n    <div class=\"row g-4\">\n      <div class=\"col-md-6\">\n        <div class=\"card h-100\">\n          <div class=\"card-header\">ES Modules (modern)</div>\n          <div class=\"card-body\">\n            <pre><code><span class=\"cm\">// package.json â†’ \"type\": \"module\"</span>\n<span class=\"kw\">import</span> express <span class=\"kw\">from</span> <span class=\"str\">'express'</span>;\n<span class=\"kw\">import</span> { readFile } <span class=\"kw\">from</span> <span class=\"str\">'node:fs/promises'</span>;\n\n<span class=\"kw\">export const</span> <span class=\"fn\">greet</span> = (name) =&gt; <span class=\"str\">`Hi </span><span class=\"tp\">${name}</span><span class=\"str\">`</span>;\n<span class=\"kw\">export default</span> app;</code></pre>\n          </div>\n        </div>\n      </div>\n      <div class=\"col-md-6\">\n        <div class=\"card h-100\">\n          <div class=\"card-header\">CommonJS (legacy)</div>\n          <div class=\"card-body\">\n            <pre><code><span class=\"cm\">// .cjs files or default package.json</span>\n<span class=\"kw\">const</span> express = require(<span class=\"str\">'express'</span>);\n<span class=\"kw\">const</span> { readFile } = require(<span class=\"str\">'node:fs/promises'</span>);\n\nmodule.exports.greet = <span class=\"kw\">function</span> (name) {\n  <span class=\"kw\">return</span> <span class=\"str\">`Hi </span><span class=\"tp\">${name}</span><span class=\"str\">`</span>;\n};</code></pre>\n          </div>\n        </div>\n      </div>\n    </div>\n\n    <div class=\"card\">\n      <div class=\"card-header\">Built-in modules you'll actually use</div>\n      <div class=\"card-body\">\n        <div class=\"table-responsive\">\n          <table class=\"table table-hover mb-0 cmd-table\">\n            <thead class=\"table-light\"><tr><th style=\"width:30%\">Module</th><th>Use case</th></tr></thead>\n            <tbody>\n              <tr><td>node:http</td><td>Raw HTTP server (Express wraps this)</td></tr>\n              <tr><td>node:fs / node:fs/promises</td><td>Read/write files — always the promises version</td></tr>\n              <tr><td>node:path</td><td>Join / resolve file paths cross-platform</td></tr>\n              <tr><td>node:url</td><td>Parse URLs (URL, URLSearchParams)</td></tr>\n              <tr><td>node:crypto</td><td>Hashing (bcrypt is easier, but this works) — randomUUID()</td></tr>\n              <tr><td>node:process</td><td>env vars, exit codes, signals</td></tr>\n              <tr><td>node:events</td><td>EventEmitter (core Node pattern)</td></tr>\n              <tr><td>node:test</td><td>Built-in test runner (no Jest needed)</td></tr>\n            </tbody>\n          </table>\n        </div>\n      </div>\n    </div>\n  </section>\n\n  <!-- ==================== EXPRESS BASICS ==================== -->\n  <section id=\"express\">\n    <h2><i class=\"bi bi-cpu me-2\"></i>Express Basics</h2>\n    <p>\n      Express is a tiny, unopinionated web framework. It gives you routing, middleware, and\n      <code class=\"inline\">req</code>/<code class=\"inline\">res</code> helpers — nothing else. You compose the rest.\n    </p>\n\n    <div class=\"card\">\n      <div class=\"card-header\">The Express 5 \"shapes\" you must know</div>\n      <div class=\"card-body\">\n        <pre><code><span class=\"kw\">import</span> express <span class=\"kw\">from</span> <span class=\"str\">'express'</span>;\n<span class=\"kw\">const</span> app = express();\n\napp.use(express.json());                     <span class=\"cm\">// middleware: parse JSON bodies</span>\napp.use(express.urlencoded({ extended: <span class=\"tp\">true</span> })); <span class=\"cm\">// form bodies</span>\n\napp.<span class=\"fn\">METHOD</span>(path, handler);                  <span class=\"cm\">// route:  GET/POST/PUT/PATCH/DELETE</span>\napp.<span class=\"fn\">use</span>(path, middleware);                  <span class=\"cm\">// middleware: runs for matching path</span>\n\n<span class=\"cm\">// handler signature â€” 3 args (middleware can also take next)</span>\napp.get(<span class=\"str\">'/users'</span>, (req, res) =&gt; {\n  res.json({ ok: <span class=\"tp\">true</span>, users: [] });\n});\n\n<span class=\"cm\">// Async handlers: Express 5 catches rejected promises automatically</span>\napp.get(<span class=\"str\">'/users/:id'</span>, <span class=\"kw\">async</span> (req, res) =&gt; {\n  <span class=\"kw\">const</span> user = <span class=\"kw\">await</span> db.findUser(req.params.id);\n  <span class=\"kw\">if</span> (!user) <span class=\"kw\">return</span> res.status(<span class=\"num\">404</span>).json({ error: <span class=\"str\">'Not found'</span> });\n  res.json(user);\n});</code></pre>\n      </div>\n    </div>\n\n    <div class=\"row g-4\">\n      <div class=\"col-md-6\">\n        <div class=\"card h-100\">\n          <div class=\"card-header\">req (request) â€” common props</div>\n          <div class=\"card-body\">\n            <pre><code>req.params     <span class=\"cm\">// /users/:id â†’ { id: '5' }</span>\nreq.query      <span class=\"cm\">// ?page=2 â†’ { page: '2' }</span>\nreq.body       <span class=\"cm\">// parsed JSON/form body (needs middleware)</span>\nreq.headers    <span class=\"cm\">// request headers object</span>\nreq.cookies    <span class=\"cm\">// needs cookie-parser</span>\nreq.get('x-api-key') <span class=\"cm\">// read a header</span>\nreq.path, req.method, req.ip</code></pre>\n          </div>\n        </div>\n      </div>\n      <div class=\"col-md-6\">\n        <div class=\"card h-100\">\n          <div class=\"card-header\">res (response) â€” common methods</div>\n          <div class=\"card-body\">\n            <pre><code>res.json(obj)       <span class=\"cm\">// send JSON (sets Content-Type)</span>\nres.status(code)    <span class=\"cm\">// set HTTP status, chainable</span>\nres.send(str)       <span class=\"cm\">// plain text/HTML</span>\nres.sendStatus(204) <span class=\"cm\">// status only, no body</span>\nres.set('x-api', '1') <span class=\"cm\">// custom header</span>\nres.redirect('/')   <span class=\"cm\">// 302 redirect</span>\nres.end()           <span class=\"cm\">// end response</span></code></pre>\n          </div>\n        </div>\n      </div>\n    </div>\n  </section>\n\n  <!-- ==================== MIDDLEWARE ==================== -->\n  <section id=\"middleware\">\n    <h2><i class=\"bi bi-diagram-3 me-2\"></i>The Middleware Pipeline</h2>\n    <p>\n      Middleware is a function that runs <em>between</em> receiving a request and sending a response.\n      Every request travels down a stack of functions; each one can respond, or call\n      <code class=\"inline\">next()</code> to pass control to the next. <strong>Order matters</strong> — it's the\n      single most important Express concept.\n    </p>\n\n    <div class=\"card\">\n      <div class=\"card-header\">Anatomy of a middleware function</div>\n      <div class=\"card-body\">\n        <pre><code><span class=\"kw\">const</span> <span class=\"fn\">logger</span> = (req, res, next) =&gt; {\n  console.log(<span class=\"str\">`</span><span class=\"tp\">${req.method}</span><span class=\"str\"> </span><span class=\"tp\">${req.originalUrl}</span><span class=\"str\">`</span>);\n  next();            <span class=\"cm\">// â† pass to the next function in the stack</span>\n};\n\n<span class=\"kw\">const</span> <span class=\"fn\">requireAuth</span> = (req, res, next) =&gt; {\n  <span class=\"kw\">if</span> (!req.headers.authorization) {\n    <span class=\"kw\">return</span> res.status(<span class=\"num\">401</span>).json({ error: <span class=\"str\">'Missing token'</span> });\n  }\n  next();\n};\n\napp.use(logger);                    <span class=\"cm\">// runs for EVERY request</span>\napp.use(<span class=\"str\">'/api'</span>, requireAuth);      <span class=\"cm\">// runs only for /api/* routes</span></code></pre>\n      </div>\n    </div>\n\n    <div class=\"card\">\n      <div class=\"card-header\">Common middleware (npm) — one-liners that do a lot</div>\n      <div class=\"card-body\">\n        <div class=\"table-responsive\">\n          <table class=\"table table-hover mb-0 cmd-table\">\n            <thead class=\"table-light\"><tr><th style=\"width:30%\">Package</th><th>What it adds</th></tr></thead>\n            <tbody>\n              <tr><td>morgan</td><td>Request logging to console (\"dev\" / \"combined\")</td></tr>\n              <tr><td>cors</td><td>Allow cross-origin browser requests (enable before routes)</td></tr>\n              <tr><td>helmet</td><td>Set security HTTP headers (HSTS, CSP, X-Frame-Options)</td></tr>\n              <tr><td>cookie-parser</td><td>Parse req.cookies</td></tr>\n              <tr><td>express-rate-limit</td><td>Basic brute-force protection per IP</td></tr>\n              <tr><td>compression</td><td>gzip/brotli compress responses</td></tr>\n            </tbody>\n          </table>\n        </div>\n      </div>\n    </div>\n\n    <div class=\"card\">\n      <div class=\"card-header\">Boilerplate server (the order you should copy)</div>\n      <div class=\"card-body\">\n        <pre><code><span class=\"kw\">import</span> express <span class=\"kw\">from</span> <span class=\"str\">'express'</span>;\n<span class=\"kw\">import</span> morgan <span class=\"kw\">from</span> <span class=\"str\">'morgan'</span>;\n<span class=\"kw\">import</span> cors <span class=\"kw\">from</span> <span class=\"str\">'cors'</span>;\n<span class=\"kw\">import</span> helmet <span class=\"kw\">from</span> <span class=\"str\">'helmet'</span>;\n<span class=\"kw\">import</span> userRouter <span class=\"kw\">from</span> <span class=\"str\">'./routes/users.js'</span>;\n\n<span class=\"kw\">const</span> app = express();\n\napp.use(helmet());                    <span class=\"cm\">// 1. security headers</span>\napp.use(cors());                      <span class=\"cm\">// 2. CORS</span>\napp.use(express.json());              <span class=\"cm\">// 3. parse JSON bodies</span>\napp.use(morgan(<span class=\"str\">'dev'</span>));             <span class=\"cm\">// 4. logging</span>\n\napp.use(<span class=\"str\">'/api/users'</span>, userRouter);  <span class=\"cm\">// 5. your routes</span>\n\n<span class=\"cm\">// 404 + error handler MUST be last</span>\napp.use((req, res) =&gt; res.status(<span class=\"num\">404</span>).json({ error: <span class=\"str\">'Not found'</span> }));\napp.use((err, req, res, next) =&gt; {\n  console.error(err);\n  res.status(err.status ?? <span class=\"num\">500</span>).json({ error: err.message });\n});\n\napp.listen(process.env.PORT ?? <span class=\"num\">3000</span>);</code></pre>\n      </div>\n    </div>\n  </section>\n\n  <!-- ==================== ROUTING ==================== -->\n  <section id=\"routing\">\n    <h2><i class=\"bi bi-signpost-2 me-2\"></i>Routing</h2>\n    <p>\n      Routes map an HTTP method + path to a handler. Use an <code class=\"inline\">express.Router()</code> per resource\n      and mount it on the app — the routing version of splitting Laravel routes into separate files.\n    </p>\n\n    <div class=\"card\">\n      <div class=\"card-header\">routes/users.js â€” the Router pattern</div>\n      <div class=\"card-body\">\n        <pre><code><span class=\"kw\">import</span> { Router } <span class=\"kw\">from</span> <span class=\"str\">'express'</span>;\n\n<span class=\"kw\">const</span> router = Router();\n\nrouter.<span class=\"fn\">param</span>(<span class=\"str\">'id'</span>, <span class=\"kw\">async</span> (req, res, next, id) =&gt; {\n  req.user = <span class=\"kw\">await</span> db.findUser(id);\n  <span class=\"kw\">if</span> (!req.user) <span class=\"kw\">return</span> res.status(<span class=\"num\">404</span>).json({ error: <span class=\"str\">'User not found'</span> });\n  next();\n});\n\nrouter.get(<span class=\"str\">'/'</span>, <span class=\"kw\">async</span> (req, res) =&gt; res.json(users));      <span class=\"cm\">// GET    /api/users</span>\nrouter.get(<span class=\"str\">'/:id'</span>, (req, res) =&gt; res.json(req.user));         <span class=\"cm\">// GET    /api/users/7</span>\nrouter.put(<span class=\"str\">'/:id'</span>, (req, res) =&gt; { ... });                    <span class=\"cm\">// PUT    /api/users/7</span>\nrouter.delete(<span class=\"str\">'/:id'</span>, (req, res) =&gt; { ... });                 <span class=\"cm\">// DELETE /api/users/7</span>\n\n<span class=\"kw\">export default</span> router;</code></pre>\n      </div>\n    </div>\n\n    <div class=\"card\">\n      <div class=\"card-header\">Route path patterns</div>\n      <div class=\"card-body\">\n        <div class=\"table-responsive\">\n          <table class=\"table table-hover mb-0 cmd-table\">\n            <thead class=\"table-light\"><tr><th style=\"width:40%\">Pattern</th><th>Matches</th></tr></thead>\n            <tbody>\n              <tr><td>/users/:id</td><td>/users/5 â†’ req.params.id = \"5\"</td></tr>\n              <tr><td>/users/:id/edit</td><td>/users/5/edit (static segments mix)</td></tr>\n              <tr><td>/files/*</td><td>/files/anything/here (wildcard, Express 5 uses /*)</td></tr>\n              <tr><td>app.all('/x', h)</td><td>Any HTTP method on /x</td></tr>\n              <tr><td>router.route('/x').get().post()</td><td>Chain methods on one path</td></tr>\n            </tbody>\n          </table>\n        </div>\n        <div class=\"warn-box\">\n          <strong>Express 5 note:</strong> the old string wildcard <code class=\"inline\">*</code> was replaced — use named\n          wildcards like <code class=\"inline\">/files/*splat</code> or a regex. Regex patterns still work:\n          <code class=\"inline\">/^\\/api\\/v\\d+/</code>.\n        </div>\n      </div>\n    </div>\n  </section>\n\n  <!-- ==================== REST ==================== -->\n  <section id=\"rest\">\n    <h2><i class=\"bi bi-broadcast me-2\"></i>REST API Design</h2>\n    <p>\n      REST is a set of conventions, not a standard. Stay consistent and your API becomes predictable.\n      The rules below are the ones that survive in production.\n    </p>\n\n    <div class=\"card\">\n      <div class=\"card-header\">Resource naming &amp; HTTP verbs</div>\n      <div class=\"card-body\">\n        <div class=\"table-responsive\">\n          <table class=\"table table-hover mb-0\">\n            <thead class=\"table-light\"><tr><th style=\"width:6%\"></th><th style=\"width:34%\">Route</th><th>Meaning</th></tr></thead>\n            <tbody>\n              <tr><td><span class=\"method-badge m-get\">GET</span></td><td>/api/users</td><td>List users (collection)</td></tr>\n              <tr><td><span class=\"method-badge m-get\">GET</span></td><td>/api/users/:id</td><td>Fetch one user</td></tr>\n              <tr><td><span class=\"method-badge m-post\">POST</span></td><td>/api/users</td><td>Create a user (body = new record)</td></tr>\n              <tr><td><span class=\"method-badge m-put\">PUT</span></td><td>/api/users/:id</td><td>Replace a user (full resource)</td></tr>\n              <tr><td><span class=\"method-badge m-patch\">PATCH</span></td><td>/api/users/:id</td><td>Partially update a user</td></tr>\n              <tr><td><span class=\"method-badge m-del\">DELETE</span></td><td>/api/users/:id</td><td>Delete a user</td></tr>\n            </tbody>\n          </table>\n        </div>\n      </div>\n    </div>\n\n    <div class=\"row g-4\">\n      <div class=\"col-md-6\">\n        <div class=\"card h-100\">\n          <div class=\"card-header\">Good practice</div>\n          <div class=\"card-body\">\n            <ul class=\"mb-0\">\n              <li class=\"mb-2\"><i class=\"bi bi-check-circle-fill me-2\" style=\"color:var(--node-green)\"></i>Plural nouns: <code class=\"inline\">/users</code>, not <code class=\"inline\">/user</code></li>\n              <li class=\"mb-2\"><i class=\"bi bi-check-circle-fill me-2\" style=\"color:var(--node-green)\"></i>Version the API: <code class=\"inline\">/api/v1/users</code></li>\n              <li class=\"mb-2\"><i class=\"bi bi-check-circle-fill me-2\" style=\"color:var(--node-green)\"></i>Nested only for true ownership: <code class=\"inline\">/users/:id/posts</code></li>\n              <li class=\"mb-2\"><i class=\"bi bi-check-circle-fill me-2\" style=\"color:var(--node-green)\"></i>Use query params for filters/pagination, not verbs</li>\n              <li class=\"mb-2\"><i class=\"bi bi-check-circle-fill me-2\" style=\"color:var(--node-green)\"></i>Use the correct status codes (table below)</li>\n            </ul>\n          </div>\n        </div>\n      </div>\n      <div class=\"col-md-6\">\n        <div class=\"card h-100\">\n          <div class=\"card-header\">Avoid</div>\n          <div class=\"card-body\">\n            <ul class=\"mb-0\">\n              <li class=\"mb-2\"><i class=\"bi bi-x-circle-fill me-2 text-danger\"></i>Verbs in URLs: <code class=\"inline\">/getUser</code>, <code class=\"inline\">/createUser</code></li>\n              <li class=\"mb-2\"><i class=\"bi bi-x-circle-fill me-2 text-danger\"></i>GET with a body (most clients can't send one)</li>\n              <li class=\"mb-2\"><i class=\"bi bi-x-circle-fill me-2 text-danger\"></i>Returning <code class=\"inline\">200</code> for everything — even failures</li>\n              <li class=\"mb-2\"><i class=\"bi bi-x-circle-fill me-2 text-danger\"></i>Storing passwords in plain text</li>\n              <li class=\"mb-2\"><i class=\"bi bi-x-circle-fill me-2 text-danger\"></i>Exposing stack traces in error responses</li>\n            </ul>\n          </div>\n        </div>\n      </div>\n    </div>\n\n    <div class=\"card\">\n      <div class=\"card-header\">Status codes cheat</div>\n      <div class=\"card-body\">\n        <div class=\"table-responsive\">\n          <table class=\"table table-hover mb-0\">\n            <thead class=\"table-light\"><tr><th style=\"width:14%\">Code</th><th>Meaning</th><th>When</th></tr></thead>\n            <tbody>\n              <tr><td><span class=\"method-badge m-get\">200</span></td><td>OK</td><td>GET, PUT, PATCH success</td></tr>\n              <tr><td><span class=\"method-badge m-post\">201</span></td><td>Created</td><td>POST success — include <code class=\"inline\">Location</code> header</td></tr>\n              <tr><td><span class=\"method-badge m-get\">204</span></td><td>No Content</td><td>DELETE success, empty body</td></tr>\n              <tr><td><span class=\"method-badge m-get\">400</span></td><td>Bad Request</td><td>Invalid body / missing field</td></tr>\n              <tr><td><span class=\"method-badge m-get\">401</span></td><td>Unauthorized</td><td>Missing / invalid credentials</td></tr>\n              <tr><td><span class=\"method-badge m-get\">403</span></td><td>Forbidden</td><td>Authenticated but not allowed</td></tr>\n              <tr><td><span class=\"method-badge m-get\">404</span></td><td>Not Found</td><td>Resource / route doesn't exist</td></tr>\n              <tr><td><span class=\"method-badge m-get\">409</span></td><td>Conflict</td><td>Duplicate, state conflict (e.g. unique email)</td></tr>\n              <tr><td><span class=\"method-badge m-del\">422</span></td><td>Unprocessable</td><td>Validation rules failed</td></tr>\n              <tr><td><span class=\"method-badge m-del\">429</span></td><td>Too Many Requests</td><td>Rate limited</td></tr>\n              <tr><td><span class=\"method-badge m-del\">500</span></td><td>Server Error</td><td>Unexpected exception</td></tr>\n            </tbody>\n          </table>\n        </div>\n      </div>\n    </div>\n  </section>\n\n  <!-- ==================== CRUD ==================== -->\n  <section id=\"crud\">\n    <h2><i class=\"bi bi-list-check me-2\"></i>Full CRUD Example</h2>\n    <p>\n      A complete, runnable in-memory CRUD API. The patterns here — route params, body parsing,\n      status codes, 404s — are exactly what you'll reproduce against a real database.\n    </p>\n\n    <div class=\"card\">\n      <div class=\"card-header\">src/index.js â€” complete app</div>\n      <div class=\"card-body\">\n        <pre><code><span class=\"kw\">import</span> express <span class=\"kw\">from</span> <span class=\"str\">'express'</span>;\n<span class=\"kw\">import</span> { randomUUID } <span class=\"kw\">from</span> <span class=\"str\">'node:crypto'</span>;\n\n<span class=\"kw\">const</span> app = express();\napp.use(express.json());\n\n<span class=\"cm\">// in-memory store (swap for MySQL later)</span>\n<span class=\"kw\">const</span> users = [\n  { id: <span class=\"str\">'a1'</span>, name: <span class=\"str\">'Ada'</span>, role: <span class=\"str\">'admin'</span> },\n  { id: <span class=\"str\">'b2'</span>, name: <span class=\"str\">'Linus'</span>, role: <span class=\"str\">'dev'</span> },\n];\n\n<span class=\"cm\">// LIST</span>\napp.get(<span class=\"str\">'/api/users'</span>, (req, res) =&gt; res.json(users));\n\n<span class=\"cm\">// CREATE</span>\napp.post(<span class=\"str\">'/api/users'</span>, (req, res) =&gt; {\n  <span class=\"kw\">const</span> { name, role } = req.body;\n  <span class=\"kw\">if</span> (!name) <span class=\"kw\">return</span> res.status(<span class=\"num\">400</span>).json({ error: <span class=\"str\">'name is required'</span> });\n  <span class=\"kw\">const</span> user = { id: randomUUID(), name, role: role ?? <span class=\"str\">'dev'</span> };\n  users.push(user);\n  res.status(<span class=\"num\">201</span>).json(user);\n});\n\n<span class=\"cm\">// READ ONE</span>\napp.get(<span class=\"str\">'/api/users/:id'</span>, (req, res) =&gt; {\n  <span class=\"kw\">const</span> user = users.find(u =&gt; u.id === req.params.id);\n  <span class=\"kw\">if</span> (!user) <span class=\"kw\">return</span> res.status(<span class=\"num\">404</span>).json({ error: <span class=\"str\">'User not found'</span> });\n  res.json(user);\n});\n\n<span class=\"cm\">// UPDATE</span>\napp.patch(<span class=\"str\">'/api/users/:id'</span>, (req, res) =&gt; {\n  <span class=\"kw\">const</span> user = users.find(u =&gt; u.id === req.params.id);\n  <span class=\"kw\">if</span> (!user) <span class=\"kw\">return</span> res.status(<span class=\"num\">404</span>).json({ error: <span class=\"str\">'User not found'</span> });\n  Object.assign(user, req.body);\n  res.json(user);\n});\n\n<span class=\"cm\">// DELETE</span>\napp.delete(<span class=\"str\">'/api/users/:id'</span>, (req, res) =&gt; {\n  <span class=\"kw\">const</span> idx = users.findIndex(u =&gt; u.id === req.params.id);\n  <span class=\"kw\">if</span> (idx === -<span class=\"num\">1</span>) <span class=\"kw\">return</span> res.status(<span class=\"num\">404</span>).json({ error: <span class=\"str\">'User not found'</span> });\n  users.splice(idx, <span class=\"num\">1</span>);\n  res.sendStatus(<span class=\"num\">204</span>);\n});\n\napp.listen(<span class=\"num\">3000</span>, () =&gt; console.log(<span class=\"str\">'API on :3000'</span>));</code></pre>\n      </div>\n    </div>\n\n    <div class=\"card\">\n      <div class=\"card-header\">Test it with curl (or the browser for GET)</div>\n      <div class=\"card-body\">\n        <pre><code>curl http://localhost:3000/api/users\ncurl -X POST http://localhost:3000/api/users \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\"name\":\"Grace\"}'\ncurl http://localhost:3000/api/users/a1\ncurl -X DELETE http://localhost:3000/api/users/a1</code></pre>\n      </div>\n    </div>\n  </section>\n\n  <!-- ==================== VALIDATION ==================== -->\n  <section id=\"validation\">\n    <h2><i class=\"bi bi-check2-square me-2\"></i>Validation</h2>\n    <p>\n      Never trust <code class=\"inline\">req.body</code>. Validate input before it touches your database.\n      <code class=\"inline\">zod</code> is the modern default — schema + type inference in one.\n    </p>\n\n    <div class=\"card\">\n      <div class=\"card-header\">zod schema + async handler</div>\n      <div class=\"card-body\">\n        <pre><code>npm install zod\n\n<span class=\"kw\">import</span> { z } <span class=\"kw\">from</span> <span class=\"str\">'zod'</span>;\n\n<span class=\"kw\">const</span> CreateUser = z.object({\n  name: z.string().min(<span class=\"num\">2</span>).max(<span class=\"num\">80</span>),\n  email: z.string().email(),\n  age: z.coerce.number().int().min(<span class=\"num\">18</span>).optional(),\n  role: z.enum([<span class=\"str\">'admin'</span>, <span class=\"str\">'dev'</span>, <span class=\"str\">'viewer'</span>]).default(<span class=\"str\">'dev'</span>),\n});\n\napp.post(<span class=\"str\">'/api/users'</span>, (req, res) =&gt; {\n  <span class=\"kw\">const</span> parsed = CreateUser.safeParse(req.body);\n\n  <span class=\"kw\">if</span> (!parsed.success) {\n    <span class=\"kw\">return</span> res.status(<span class=\"num\">422</span>).json({\n      error: <span class=\"str\">'Validation failed'</span>,\n      issues: parsed.error.flatten().fieldErrors,\n    });\n  }\n  <span class=\"kw\">const</span> user = createUser(parsed.data);   <span class=\"cm\">// parsed.data is fully typed</span>\n  res.status(<span class=\"num\">201</span>).json(user);\n});</code></pre>\n      </div>\n    </div>\n\n    <div class=\"warn-box\">\n      <strong>422 vs 400:</strong> use <code class=\"inline\">400</code> for malformed JSON or wrong types, and\n      <code class=\"inline\">422</code> when the request is well-formed but fails your business rules.\n    </div>\n  </section>\n\n  <!-- ==================== ERRORS ==================== -->\n  <section id=\"errors\">\n    <h2><i class=\"bi bi-exclamation-octagon me-2\"></i>Error Handling</h2>\n    <p>\n      Error middleware has <strong>4 arguments</strong> — Express identifies it by that signature.\n      It must be registered last, after all routes.\n    </p>\n\n    <div class=\"card\">\n      <div class=\"card-header\">Centralized error handler</div>\n      <div class=\"card-body\">\n        <pre><code><span class=\"cm\">// errors.js â€” a small helper</span>\n<span class=\"kw\">export class</span> <span class=\"fn\">HttpError</span> <span class=\"kw\">extends</span> Error {\n  <span class=\"kw\">constructor</span>(status, message) {\n    <span class=\"kw\">super</span>(message);\n    <span class=\"kw\">this</span>.status = status;\n  }\n}\n\n<span class=\"cm\">// throw anywhere, Express 5 routes async errors here automatically</span>\n<span class=\"kw\">import</span> { HttpError } <span class=\"kw\">from</span> <span class=\"str\">'./errors.js'</span>;\n\napp.get(<span class=\"str\">'/api/users/:id'</span>, <span class=\"kw\">async</span> (req, res) =&gt; {\n  <span class=\"kw\">const</span> user = <span class=\"kw\">await</span> db.findUser(req.params.id);\n  <span class=\"kw\">if</span> (!user) <span class=\"kw\">throw new</span> HttpError(<span class=\"num\">404</span>, <span class=\"str\">'User not found'</span>);\n  res.json(user);\n});\n\n<span class=\"cm\">// last middleware â€” the one true error handler</span>\napp.use((err, req, res, next) =&gt; {\n  console.error(<span class=\"str\">'ERR'</span>, err);                 <span class=\"cm\">// always log full error server-side</span>\n  res.status(err.status ?? <span class=\"num\">500</span>).json({\n    error: err.status ? err.message : <span class=\"str\">'Internal server error'</span>,\n  });\n});</code></pre>\n      </div>\n    </div>\n\n    <div class=\"highlight-box\">\n      <strong>Never leak internals.</strong> Only send <code class=\"inline\">err.message</code> when you set\n      <code class=\"inline\">err.status</code> yourself. For unexpected errors, reply with a generic message and\n      keep the real stack trace in the server logs.\n    </div>\n  </section>\n\n  <!-- ==================== DATABASE ==================== -->\n  <section id=\"database\">\n    <h2><i class=\"bi bi-database me-2\"></i>Database (MySQL)</h2>\n    <p>\n      Two common paths: the <code class=\"inline\">mysql2</code> driver with <code class=\"inline\">execute()</code>\n      prepared statements (like PDO), or an ORM/query builder like Prisma, Drizzle, or Knex.\n      For a PHP developer, <code class=\"inline\">mysql2</code> prepared statements feel the most familiar.\n    </p>\n\n    <div class=\"card\">\n      <div class=\"card-header\">mysql2 pool + prepared statements</div>\n      <div class=\"card-body\">\n        <pre><code>npm install mysql2 dotenv\n\n<span class=\"cm\">// db.js</span>\n<span class=\"kw\">import</span> mysql <span class=\"kw\">from</span> <span class=\"str\">'mysql2/promise'</span>;\n\n<span class=\"kw\">export const</span> pool = mysql.createPool({\n  host: process.env.DB_HOST,\n  user: process.env.DB_USER,\n  password: process.env.DB_PASS,\n  database: process.env.DB_NAME,\n  waitForConnections: <span class=\"tp\">true</span>,\n  connectionLimit: <span class=\"num\">10</span>,\n});\n\n<span class=\"cm\">// route â€” async/await, prepared statements</span>\napp.get(<span class=\"str\">'/api/users'</span>, <span class=\"kw\">async</span> (req, res) =&gt; {\n  <span class=\"kw\">const</span> [rows] = <span class=\"kw\">await</span> pool.execute(\n    <span class=\"str\">'SELECT id, name, email FROM users WHERE active = ? ORDER BY id DESC LIMIT ?'</span>,\n    [<span class=\"num\">1</span>, <span class=\"num\">50</span>]\n  );\n  res.json(rows);\n});\n\n<span class=\"cm\">// .env (never commit this file)</span>\n<span class=\"cm\">// DB_HOST=localhost</span>\n<span class=\"cm\">// DB_USER=root</span>\n<span class=\"cm\">// DB_PASS=secret</span>\n<span class=\"cm\">// DB_NAME=myapp</span></code></pre>\n      </div>\n    </div>\n\n    <div class=\"card\">\n      <div class=\"card-header\">mysql2 vs ORM (Prisma)</div>\n      <div class=\"card-body\">\n        <div class=\"table-responsive\">\n          <table class=\"table table-hover mb-0\">\n            <thead class=\"table-light\"><tr><th style=\"width:22%\"></th><th>mysql2 (raw SQL)</th><th>Prisma (ORM)</th></tr></thead>\n            <tbody>\n              <tr><td><strong>Style</strong></td><td>SQL strings, like PDO</td><td>Type-safe query builder</td></tr>\n              <tr><td><strong>Migrations</strong></td><td>You write them</td><td>prisma migrate</td></tr>\n              <tr><td><strong>Type safety</strong></td><td>Manual</td><td>Generated types</td></tr>\n              <tr><td><strong>Best for</strong></td><td>Complex queries, teams who know SQL</td><td>Rapid CRUD, TS projects</td></tr>\n            </tbody>\n          </table>\n        </div>\n      </div>\n    </div>\n  </section>\n\n  <!-- ==================== AUTH ==================== -->\n  <section id=\"auth\">\n    <h2><i class=\"bi bi-shield-lock me-2\"></i>Auth &amp; JWT</h2>\n    <p>\n      The standard stateless API flow: <code class=\"inline\">bcryptjs</code> to hash passwords,\n      <code class=\"inline\">jsonwebtoken</code> to sign tokens, a middleware to verify them.\n    </p>\n\n    <div class=\"card\">\n      <div class=\"card-header\">Login â†’ token â†’ protected route</div>\n      <div class=\"card-body\">\n        <pre><code>npm install bcryptjs jsonwebtoken\n\n<span class=\"kw\">import</span> bcrypt <span class=\"kw\">from</span> <span class=\"str\">'bcryptjs'</span>;\n<span class=\"kw\">import</span> jwt <span class=\"kw\">from</span> <span class=\"str\">'jsonwebtoken'</span>;\n\n<span class=\"cm\">// register â€” hash the password, NEVER store plain text</span>\n<span class=\"kw\">const</span> hash = <span class=\"kw\">await</span> bcrypt.hash(req.body.password, <span class=\"num\">10</span>);\n\n<span class=\"cm\">// login â€” compare, then sign</span>\n<span class=\"kw\">const</span> ok = <span class=\"kw\">await</span> bcrypt.compare(req.body.password, user.password_hash);\n<span class=\"kw\">if</span> (!ok) <span class=\"kw\">return</span> res.status(<span class=\"num\">401</span>).json({ error: <span class=\"str\">'Invalid credentials'</span> });\n\n<span class=\"kw\">const</span> token = jwt.sign(\n  { sub: user.id, role: user.role },\n  process.env.JWT_SECRET,\n  { expiresIn: <span class=\"str\">'1d'</span> }\n);\nres.json({ token });\n\n<span class=\"cm\">// protect middleware â€” verify on every request</span>\n<span class=\"kw\">const</span> <span class=\"fn\">auth</span> = (req, res, next) =&gt; {\n  <span class=\"kw\">const</span> [ , token ] = req.headers.authorization?.split(<span class=\"str\">' '</span>) ?? [];\n  <span class=\"kw\">try</span> {\n    req.user = jwt.verify(token, process.env.JWT_SECRET);\n    next();\n  } <span class=\"kw\">catch</span> {\n    res.status(<span class=\"num\">401</span>).json({ error: <span class=\"str\">'Invalid or expired token'</span> });\n  }\n};\n\napp.get(<span class=\"str\">'/api/me'</span>, auth, (req, res) =&gt; res.json(req.user));</code></pre>\n      </div>\n    </div>\n\n    <div class=\"warn-box\">\n      <strong>Security floor:</strong> put <code class=\"inline\">JWT_SECRET</code> in <code class=\"inline\">.env</code>, never in code.\n      Prefer short-lived tokens + refresh tokens for real apps. Add\n      <code class=\"inline\">express-rate-limit</code> on login to slow brute-force attempts.\n    </div>\n  </section>\n\n  <!-- ==================== TESTING ==================== -->\n  <section id=\"testing\">\n    <h2><i class=\"bi bi-check-circle me-2\"></i>Testing</h2>\n    <p>\n      Node ships a built-in test runner (<code class=\"inline\">node:test</code>). Pair it with\n      <code class=\"inline\">supertest</code> to hit your Express app without opening a port.\n    </p>\n\n    <div class=\"card\">\n      <div class=\"card-header\">API tests with node:test + supertest</div>\n      <div class=\"card-body\">\n        <pre><code>npm install --save-dev supertest\n\n<span class=\"cm\">// index.test.js  â†’  npm test runs it</span>\n<span class=\"kw\">import</span> { test, describe } <span class=\"kw\">from</span> <span class=\"str\">'node:test'</span>;\n<span class=\"kw\">import</span> assert <span class=\"kw\">from</span> <span class=\"str\">'node:assert/strict'</span>;\n<span class=\"kw\">import</span> request <span class=\"kw\">from</span> <span class=\"str\">'supertest'</span>;\n<span class=\"kw\">import</span> app <span class=\"kw\">from</span> <span class=\"str\">'./index.js'</span>;\n\ndescribe(<span class=\"str\">'POST /api/users'</span>, () =&gt; {\n  test(<span class=\"str\">'creates a user'</span>, <span class=\"kw\">async</span> () =&gt; {\n    <span class=\"kw\">const</span> res = <span class=\"kw\">await</span> request(app)\n      .post(<span class=\"str\">'/api/users'</span>)\n      .send({ name: <span class=\"str\">'Grace'</span> });\n    assert.equal(res.status, <span class=\"num\">201</span>);\n    assert.ok(res.body.id);\n  });\n\n  test(<span class=\"str\">'rejects missing name'</span>, <span class=\"kw\">async</span> () =&gt; {\n    <span class=\"kw\">const</span> res = <span class=\"kw\">await</span> request(app).post(<span class=\"str\">'/api/users'</span>).send({});\n    assert.equal(res.status, <span class=\"num\">400</span>);\n  });\n});</code></pre>\n      </div>\n    </div>\n\n    <div class=\"highlight-box\">\n      Export the <code class=\"inline\">app</code> separately from the <code class=\"inline\">app.listen()</code> call so tests can\n      import it without binding a port: <code class=\"inline\">export default app</code> in one file, listen in\n      <code class=\"inline\">server.js</code>.\n    </div>\n  </section>\n\n  <!-- ==================== DEPLOYMENT ==================== -->\n  <section id=\"deployment\">\n    <h2><i class=\"bi bi-rocket-takeoff me-2\"></i>Deployment</h2>\n\n    <div class=\"card\">\n      <div class=\"card-header\">Process management (PM2)</div>\n      <div class=\"card-body\">\n        <pre><code>npm install -g pm2\n\npm2 start src/index.js --name api --env production\npm2 save &amp;&amp; pm2 startup          <span class=\"cm\"># restart on reboot</span>\npm2 logs api\npm2 reload api                   <span class=\"cm\"># zero-downtime restart</span></code></pre>\n      </div>\n    </div>\n\n    <div class=\"row g-4\">\n      <div class=\"col-md-6\">\n        <div class=\"card h-100\">\n          <div class=\"card-header\">Production checklist</div>\n          <div class=\"card-body\">\n            <ul class=\"mb-0\">\n              <li class=\"mb-2\"><i class=\"bi bi-check-circle-fill me-2\" style=\"color:var(--node-green)\"></i>Set <code class=\"inline\">NODE_ENV=production</code> (Express enables caching)</li>\n              <li class=\"mb-2\"><i class=\"bi bi-check-circle-fill me-2\" style=\"color:var(--node-green)\"></i>Reverse proxy: nginx or Caddy in front</li>\n              <li class=\"mb-2\"><i class=\"bi bi-check-circle-fill me-2\" style=\"color:var(--node-green)\"></i>HTTPS (Let's Encrypt) + helmet</li>\n              <li class=\"mb-2\"><i class=\"bi bi-check-circle-fill me-2\" style=\"color:var(--node-green)\"></i>Secrets in <code class=\"inline\">.env</code>, never committed</li>\n              <li class=\"mb-2\"><i class=\"bi bi-check-circle-fill me-2\" style=\"color:var(--node-green)\"></i>Run behind PM2 with auto-restart</li>\n              <li class=\"mb-2\"><i class=\"bi bi-check-circle-fill me-2\" style=\"color:var(--node-green)\"></i>Add a health endpoint: <code class=\"inline\">GET /health</code></li>\n            </ul>\n          </div>\n        </div>\n      </div>\n      <div class=\"col-md-6\">\n        <div class=\"card h-100\">\n          <div class=\"card-header\">Hosting options</div>\n          <div class=\"card-body\">\n            <ul class=\"mb-0\">\n              <li class=\"mb-2\"><i class=\"bi bi-arrow-right-circle me-2\" style=\"color:var(--node-green)\"></i><strong>VPS</strong> (DigitalOcean/Vultr) â€” full control, PM2 + nginx</li>\n              <li class=\"mb-2\"><i class=\"bi bi-arrow-right-circle me-2\" style=\"color:var(--node-green)\"></i><strong>PaaS</strong> (Render, Railway, Fly.io) â€” git push deploys</li>\n              <li class=\"mb-2\"><i class=\"bi bi-arrow-right-circle me-2\" style=\"color:var(--node-green)\"></i><strong>Serverless</strong> (Vercel, AWS Lambda) â€” Express via adapter</li>\n              <li class=\"mb-2\"><i class=\"bi bi-arrow-right-circle me-2\" style=\"color:var(--node-green)\"></i><strong>Docker</strong> â€” <code class=\"inline\">node:22-slim</code> image + PORT 3000</li>\n            </ul>\n          </div>\n        </div>\n      </div>\n    </div>\n  </section>\n\n  <!-- ==================== CHEATSHEET ==================== -->\n  <section id=\"cheatsheet\">\n    <h2><i class=\"bi bi-stars me-2\"></i>Cheat Sheet</h2>\n\n    <div class=\"card\">\n      <div class=\"card-header\">Express 5 quick reference</div>\n      <div class=\"card-body\">\n        <div class=\"table-responsive\">\n          <table class=\"table table-hover mb-0 cmd-table\">\n            <thead class=\"table-light\"><tr><th style=\"width:34%\">Code</th><th>Does what</th></tr></thead>\n            <tbody>\n              <tr><td>app.use(mw)</td><td>Run middleware for every request</td></tr>\n              <tr><td>app.use('/path', mw)</td><td>Run middleware only under a path</td></tr>\n              <tr><td>app.get('/p', h)</td><td>Route for GET</td></tr>\n              <tr><td>app.post('/p', h)</td><td>Route for POST</td></tr>\n              <tr><td>req.params.id</td><td>Route parameter value</td></tr>\n              <tr><td>req.query.page</td><td>Query string value</td></tr>\n              <tr><td>req.body</td><td>Parsed request body (after express.json())</td></tr>\n              <tr><td>res.json(obj)</td><td>Send JSON response</td></tr>\n              <tr><td>res.status(n).json(obj)</td><td>Send status + JSON</td></tr>\n              <tr><td>next()</td><td>Pass control to next middleware</td></tr>\n              <tr><td>next(err)</td><td>Jump straight to the error handler</td></tr>\n              <tr><td>router.param('id', f)</td><td>Run code before a :id route</td></tr>\n            </tbody>\n          </table>\n        </div>\n      </div>\n    </div>\n\n    <div class=\"card\">\n      <div class=\"card-header\">Common npm packages, one line each</div>\n      <div class=\"card-body\">\n        <div class=\"table-responsive\">\n          <table class=\"table table-hover mb-0 cmd-table\">\n            <thead class=\"table-light\"><tr><th style=\"width:34%\">Package</th><th>Job</th></tr></thead>\n            <tbody>\n              <tr><td>express</td><td>HTTP framework (routing + middleware)</td></tr>\n              <tr><td>morgan</td><td>Request logs</td></tr>\n              <tr><td>cors</td><td>Cross-origin requests</td></tr>\n              <tr><td>helmet</td><td>Security headers</td></tr>\n              <tr><td>zod</td><td>Input validation + types</td></tr>\n              <tr><td>mysql2</td><td>MySQL driver (promises + pool)</td></tr>\n              <tr><td>prisma</td><td>ORM + migrations</td></tr>\n              <tr><td>bcryptjs</td><td>Password hashing (pure JS, no build)</td></tr>\n              <tr><td>jsonwebtoken</td><td>Sign / verify JWTs</td></tr>\n              <tr><td>dotenv</td><td>Load .env into process.env</td></tr>\n              <tr><td>nodemon</td><td>Dev auto-restart</td></tr>\n              <tr><td>supertest</td><td>HTTP assertions for tests</td></tr>\n              <tr><td>pm2</td><td>Production process manager</td></tr>\n            </tbody>\n          </table>\n        </div>\n      </div>\n    </div>\n\n    <div class=\"highlight-box\">\n      <strong>The one-line summary:</strong> Express is a queue of functions. Parse the body,\n      log, authenticate, route, respond — and keep the error handler for last.\n      Everything else is just npm installs.\n    </div>\n  </section>\n\n  <!-- ==================== FOOTER ==================== -->\n  <footer class=\"mt-5 pt-4\">\n    <div class=\"row\">\n      <div class=\"col-md-6\">\n        <p class=\"fw-bold mb-2\" style=\"font-family:'Space Grotesk',sans-serif\">Node.js + Express Quick Reference</p>\n        <p class=\"small text-muted\">Built for study. Always verify against the official documentation — Express 5, Node.js 22.</p>\n      </div>\n      <div class=\"col-md-6 text-md-end\">\n        <p class=\"small mb-1\">\n          <a href=\"https://expressjs.com\" target=\"_blank\" class=\"text-decoration-none\"><i class=\"bi bi-book me-1\"></i> Express Docs</a>\n          ·\n          <a href=\"https://nodejs.org/docs\" target=\"_blank\" class=\"text-decoration-none\"><i class=\"bi bi-hdd-network me-1\"></i> Node Docs</a>\n        </p>\n        <p class=\"small text-muted\">© 2026 Study Guide</p>\n      </div>\n    </div>\n  </footer>\n\n"}