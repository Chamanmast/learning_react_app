{"slug":"ls","title":"ls Command â€” Complete Reference (Linux, macOS, CMD & PowerShell)","acc":"#e8590c","sidebar":"\n  <div class=\"brand\">\n    <h5><i class=\"bi bi-terminal-fill me-2\" style=\"color:var(--term-green)\"></i>ls Reference</h5>\n    <small class=\"d-block mt-1\">List directory contents</small>\n  </div>\n  <div class=\"py-2\">\n    <div class=\"nav-section\">Basics</div>\n    <a class=\"nav-link\" href=\"#intro\"><i class=\"bi bi-info-circle\"></i> Introduction</a>\n    <a class=\"nav-link\" href=\"#basic\"><i class=\"bi bi-play-circle\"></i> Basic Usage</a>\n    <a class=\"nav-link\" href=\"#format\"><i class=\"bi bi-card-list\"></i> Output Formats</a>\n\n    <div class=\"nav-section\">Options</div>\n    <a class=\"nav-link\" href=\"#flags\"><i class=\"bi bi-flag\"></i> All Flags (a–z)</a>\n    <a class=\"nav-link\" href=\"#long\"><i class=\"bi bi-list-ol\"></i> Long Format (ls -l)</a>\n    <a class=\"nav-link\" href=\"#hidden\"><i class=\"bi bi-eye-slash\"></i> Hidden Files</a>\n    <a class=\"nav-link\" href=\"#sort\"><i class=\"bi bi-sort-down\"></i> Sorting</a>\n    <a class=\"nav-link\" href=\"#recursive\"><i class=\"bi bi-diagram-3\"></i> Recursive</a>\n    <a class=\"nav-link\" href=\"#filter\"><i class=\"bi bi-funnel\"></i> Filtering / Glob</a>\n\n    <div class=\"nav-section\">Windows</div>\n    <a class=\"nav-link\" href=\"#cmd\"><i class=\"bi bi-windows\"></i> CMD (dir)</a>\n    <a class=\"nav-link\" href=\"#powershell\"><i class=\"bi bi-lightning\"></i> PowerShell</a>\n    <a class=\"nav-link\" href=\"#wsl\"><i class=\"bi bi-box\"></i> WSL / Git Bash</a>\n\n    <div class=\"nav-section\">Practical</div>\n    <a class=\"nav-link\" href=\"#recipes\"><i class=\"bi bi-bookmark-star\"></i> Recipes</a>\n    <a class=\"nav-link\" href=\"#tips\"><i class=\"bi bi-lightbulb\"></i> Pro Tips</a>\n    <a class=\"nav-link\" href=\"#errors\"><i class=\"bi bi-exclamation-triangle\"></i> Common Errors</a>\n  </div>\n","content":"\n\n  <!-- HERO -->\n  <div class=\"d-flex align-items-center gap-3 mb-4 flex-wrap\">\n    <div>\n      <h1 class=\"fw-bold mb-1\" style=\"color:#0f172a\">\n        <i class=\"bi bi-terminal-fill\" style=\"color:var(--term-green)\"></i>\n        <code style=\"color:#0f172a\">ls</code> — List Directory Contents\n      </h1>\n      <p class=\"text-muted mb-0 fs-5\">\n        Complete reference for <strong>Linux</strong>, <strong>macOS</strong>, <strong>CMD</strong> and <strong>PowerShell</strong>\n        <span class=\"badge bg-success ms-2\"><span class=\"pulse-dot\"></span>100+ examples</span>\n        <span class=\"badge bg-secondary ms-1\">POSIX</span>\n        <span class=\"badge bg-info ms-1\">Cross-platform</span>\n      </p>\n    </div>\n  </div>\n\n  <p class=\"fs-5 text-muted mb-5\">\n    The <code>ls</code> command lists files and directories in the current or specified path.\n    It is one of the most-used commands on Unix-like systems and is also available\n    on Windows through <strong>PowerShell</strong>, <strong>WSL</strong>, or <strong>Git Bash</strong>.\n    This guide covers <strong>every flag</strong>, <strong>output formats</strong>, <strong>sorting</strong>,\n    <strong>filtering</strong>, <strong>CMD equivalents</strong> (<code>dir</code>), and <strong>practical recipes</strong>.\n  </p>\n\n  <!-- ==================== INTRO ==================== -->\n  <section id=\"intro\">\n    <h2 class=\"ls-heading\"><i class=\"bi bi-info-circle me-2\"></i>Introduction</h2>\n\n    <div class=\"card\">\n      <div class=\"card-header\"><i class=\"bi bi-book me-1\"></i> What is <code>ls</code>?</div>\n      <div class=\"card-body\">\n        <p><code>ls</code> (list) is a Unix/Linux command that displays the contents of a directory. It comes from the\n        <code>coreutils</code> package on Linux and ships in nearly every Unix-like system, including macOS.</p>\n        <ul>\n          <li><strong>Type:</strong> External command</li>\n          <li><strong>Package:</strong> <code>coreutils</code> (GNU) / BSD <code>ls</code> (macOS by default)</li>\n          <li><strong>Manual:</strong> <code>man ls</code> · <code>ls --help</code></li>\n          <li><strong>Exit code:</strong> <code>0</code> on success, <code>1</code> on minor error, <code>2</code> on serious error</li>\n        </ul>\n        <div class=\"highlight-box\">\n          <strong>Where is it?</strong> — <code>which ls</code> → <code>/bin/ls</code> (Linux) · <code>/usr/bin/ls</code> (macOS) · available in WSL and Git Bash on Windows.\n        </div>\n      </div>\n    </div>\n\n    <div class=\"card\">\n      <div class=\"card-header\">Quick Start — the 5 commands you'll use 90% of the time</div>\n      <div class=\"card-body\">\n<pre class=\"term\"><code><span class=\"c-cmd\">ls</span>                    <span class=\"c-comment\"># plain list</span>\n<span class=\"c-cmd\">ls -l</span>                <span class=\"c-comment\"># long format (permissions, owner, size, date)</span>\n<span class=\"c-cmd\">ls -a</span>                <span class=\"c-comment\"># include hidden files (dotfiles)</span>\n<span class=\"c-cmd\">ls -lh</span>               <span class=\"c-comment\"># long format + human-readable sizes</span>\n<span class=\"c-cmd\">ls -la</span>               <span class=\"c-comment\"># long + hidden → the \"full\" view</span></code></pre>\n      </div>\n    </div>\n  </section>\n\n  <!-- ==================== BASIC ==================== -->\n  <section id=\"basic\">\n    <h2 class=\"ls-heading\"><i class=\"bi bi-play-circle me-2\"></i>Basic Usage</h2>\n\n    <div class=\"card\">\n      <div class=\"card-header\">Listing paths</div>\n      <div class=\"card-body\">\n<pre class=\"term\"><code><span class=\"c-cmd\">ls</span>                            <span class=\"c-comment\"># current directory</span>\n<span class=\"c-cmd\">ls /usr/local/bin</span>           <span class=\"c-comment\"># absolute path</span>\n<span class=\"c-cmd\">ls ..</span>                        <span class=\"c-comment\"># parent directory</span>\n<span class=\"c-cmd\">ls ~</span>                         <span class=\"c-comment\"># home directory</span>\n<span class=\"c-cmd\">ls /etc /var /tmp</span>           <span class=\"c-comment\"># multiple paths</span>\n<span class=\"c-cmd\">ls -d /usr/*</span>                <span class=\"c-comment\"># list directories inside /usr (not their contents)</span></code></pre>\n      </div>\n    </div>\n\n    <div class=\"card\">\n      <div class=\"card-header\">Multiple paths in one command</div>\n      <div class=\"card-body\">\n<pre class=\"term\"><code><span class=\"c-cmd\">ls</span> src/ tests/ docs/\n<span class=\"c-comment\"># src/:\n<span class=\"c-output\">  app.js  index.js  utils.js\n</span><span class=\"c-comment\"># tests/:\n<span class=\"c-output\">  app.test.js  setup.js\n</span><span class=\"c-comment\"># docs/:\n<span class=\"c-output\">  README.md  CHANGELOG.md</span></span></span></span></code></pre>\n      </div>\n    </div>\n  </section>\n\n  <!-- ==================== FORMAT ==================== -->\n  <section id=\"format\">\n    <h2 class=\"ls-heading\"><i class=\"bi bi-card-list me-2\"></i>Output Formats</h2>\n    <p>Two principal output shapes — <strong>short</strong> (default) and <strong>long</strong> (<code>-l</code>).\n       There are also comma-separated, slashes-after-dirs, and classification modes.</p>\n\n    <div class=\"row g-4\">\n      <div class=\"col-lg-6\">\n        <div class=\"card h-100\">\n          <div class=\"card-header\">Short format (default)</div>\n          <div class=\"card-body\">\n<pre class=\"term no-prompt\"><code><span class=\"<span\">\"c-output\"</span>&gt;Documents  Downloads  Music  Pictures  Videos</code></pre>\n            <p class=\"text-muted small mb-0\">Names separated by spaces, sorted alphabetically by default.</p>\n          </div>\n        </div>\n      </div>\n      <div class=\"col-lg-6\">\n        <div class=\"card h-100\">\n          <div class=\"card-header\">Long format <span class=\"flag-pill\">-l</span></div>\n          <div class=\"card-body\">\n<pre class=\"term no-prompt\"><code><span class=\"<span\">\"c-output\"</span>&gt;drwxr-xr-x  <span class=\"num\">5</span> alice staff   <span class=\"num\">160</span>B Jun  <span class=\"num\">4</span> <span class=\"num\">10</span>:<span class=\"num\">14</span> Documents\n<span class=\"fn\">-rw-r--r--</span>  <span class=\"num\">1</span> alice staff   <span class=\"num\">1.2</span>K Jun  <span class=\"num\">4</span> <span class=\"num\">09</span>:<span class=\"num\">30</span> notes.txt\nlrwxr-xr-x  <span class=\"num\">1</span> alice staff    <span class=\"num\">32</span>B Jun  <span class=\"num\">1</span> <span class=\"num\">08</span>:<span class=\"num\">00</span> link -&gt; target\n</code></pre>\n            <p class=\"text-muted small mb-0\">Permissions, links, owner, group, size, mtime, name.</p>\n          </div>\n        </div>\n      </div>\n    </div>\n\n    <div class=\"row g-4 mt-1\">\n      <div class=\"col-lg-6\">\n        <div class=\"card h-100\">\n          <div class=\"card-header\">Comma-separated <span class=\"flag-pill\">-m</span></div>\n          <div class=\"card-body\">\n<pre class=\"term no-prompt\"><code><span class=\"<span\">\"c-output\"</span>&gt;Documents, Downloads, Music, Pictures, Videos</code></pre>\n          </div>\n        </div>\n      </div>\n      <div class=\"col-lg-6\">\n        <div class=\"card h-100\">\n          <div class=\"card-header\">One per line <span class=\"flag-pill\">-1</span></div>\n          <div class=\"card-body\">\n<pre class=\"term no-prompt\"><code><span class=\"<span\">\"c-output\"</span>&gt;Documents\nDownloads\nMusic\nPictures\nVideos</code></pre>\n            <p class=\"text-muted small mb-0\">Useful for piping into other tools.</p>\n          </div>\n        </div>\n      </div>\n      <div class=\"col-lg-6\">\n        <div class=\"card h-100\">\n          <div class=\"card-header\">Classify <span class=\"flag-pill\">-F</span></div>\n          <div class=\"card-body\">\n<pre class=\"term no-prompt\"><code><span class=\"<span\">\"c-output\"</span>&gt;Documents/  Downloads/  link@  notes.txt  script*</code></pre>\n            <p class=\"text-muted small mb-0\"><code>/</code>=dir · <code>@</code>=symlink · <code>*</code>=executable · <code>=</code>=socket · <code>|</code>=FIFO</p>\n          </div>\n        </div>\n      </div>\n      <div class=\"col-lg-6\">\n        <div class=\"card h-100\">\n          <div class=\"card-header\">Colorize <span class=\"flag-pill\">--color</span></div>\n          <div class=\"card-body\">\n<pre class=\"term no-prompt\"><code><span class=\"<span\">\"c-output\"</span>&gt;$ <span style=\"<span\" class=\"str\">\"color:#60a5fa\"</span>&gt;Documents  <span style=\"<span\" class=\"str\">\"color:#60a5fa\"</span>&gt;Downloads  <span style=\"<span\" class=\"str\">\"color:#facc15\"</span>&gt;link  notes.txt  script<span style=\"<span\" class=\"str\">\"color:#86efac\"</span>&gt;*</code></pre>\n            <p class=\"text-muted small mb-0\">Auto on most distros via alias <code>ls='ls --color=auto'</code>.</p>\n          </div>\n        </div>\n      </div>\n    </div>\n  </section>\n\n  <!-- ==================== FLAGS ==================== -->\n  <section id=\"flags\">\n    <h2 class=\"ls-heading\"><i class=\"bi bi-flag me-2\"></i>All Flags Reference (a–z)</h2>\n    <p class=\"text-muted\">A consolidated alphabetised reference. GNU <code>ls</code> on Linux.\n       BSD <code>ls</code> on macOS shares most of these; BSD-only flags are noted.</p>\n\n    <div class=\"card\">\n      <div class=\"table-responsive\">\n        <table class=\"table table-hover mb-0 cmd-table\">\n          <thead class=\"table-light\"><tr><th style=\"width:34%\">Flag</th><th>Description</th></tr></thead>\n          <tbody>\n            <tr><td><span class=\"flag-pill\">-a</span> <span class=\"flag-pill long\">--all</span></td><td>Include hidden files (entries starting with <code>.</code>)</td></tr>\n            <tr><td><span class=\"flag-pill\">-A</span> <span class=\"flag-pill long\">--almost-all</span></td><td>Like <code>-a</code> but exclude <code>.</code> and <code>..</code></td></tr>\n            <tr><td><span class=\"flag-pill\">-b</span> <span class=\"flag-pill long\">--escape</span></td><td>Print C-style escapes for non-graphic characters</td></tr>\n            <tr><td><span class=\"flag-pill\">-B</span> <span class=\"flag-pill long\">--ignore-backups</span></td><td>Do not list entries ending with <code>~</code></td></tr>\n            <tr><td><span class=\"flag-pill\">-c</span></td><td>Sort by ctime; with <code>-l</code> show ctime instead of mtime</td></tr>\n            <tr><td><span class=\"flag-pill\">-C</span></td><td>List entries by columns (default when stdout is a tty)</td></tr>\n            <tr><td><span class=\"flag-pill\">-d</span> <span class=\"flag-pill long\">--directory</span></td><td>List directories themselves, not their contents</td></tr>\n            <tr><td><span class=\"flag-pill\">-F</span> <span class=\"flag-pill long\">--classify</span></td><td>Append indicator (<code>/@*=|&gt;</code>) to entries</td></tr>\n            <tr><td><span class=\"flag-pill\">-g</span></td><td>Like <code>-l</code> but omit the owner column</td></tr>\n            <tr><td><span class=\"flag-pill\">-G</span> <span class=\"flag-pill long\">--no-group</span></td><td>In long listing, don't print group names</td></tr>\n            <tr><td><span class=\"flag-pill\">-h</span> <span class=\"flag-pill long\">--human-readable</span></td><td>Print sizes in K, M, G, T, P (with <code>-l</code>)</td></tr>\n            <tr><td><span class=\"flag-pill\">-H</span> <span class=\"flag-pill long\">--dereference-command-line</span></td><td>Follow symbolic links listed on the command line</td></tr>\n            <tr><td><span class=\"flag-pill\">-i</span> <span class=\"flag-pill long\">--inode</span></td><td>Print the inode number of each file</td></tr>\n            <tr><td><span class=\"flag-pill\">-I</span> <span class=\"flag-pill long\">--ignore=PATTERN</span></td><td>Do not list entries matching shell PATTERN</td></tr>\n            <tr><td><span class=\"flag-pill\">-k</span> <span class=\"flag-pill long\">--kibibytes</span></td><td>Use 1024-byte blocks for disk usage (default since coreutils 8)</td></tr>\n            <tr><td><span class=\"flag-pill\">-l</span></td><td>Use a long listing format</td></tr>\n            <tr><td><span class=\"flag-pill\">-L</span> <span class=\"flag-pill long\">--dereference</span></td><td>Show info for the target of symbolic links</td></tr>\n            <tr><td><span class=\"flag-pill\">-m</span></td><td>Fill width with entries separated by commas</td></tr>\n            <tr><td><span class=\"flag-pill\">-n</span> <span class=\"flag-pill long\">--numeric-uid-gid</span></td><td>Like <code>-l</code> but list numeric UIDs and GIDs</td></tr>\n            <tr><td><span class=\"flag-pill\">-N</span> <span class=\"flag-pill long\">--literal</span></td><td>Print raw entry names (no quoting)</td></tr>\n            <tr><td><span class=\"flag-pill\">-o</span></td><td>Like <code>-l</code> but omit the group</td></tr>\n            <tr><td><span class=\"flag-pill\">-p</span></td><td>Append <code>/</code> indicator to directories</td></tr>\n            <tr><td><span class=\"flag-pill\">-q</span> <span class=\"flag-pill long\">--hide-control-chars</span></td><td>Print <code>?</code> for non-graphic characters</td></tr>\n            <tr><td><span class=\"flag-pill\">-Q</span> <span class=\"flag-pill long\">--quote-name</span></td><td>Enclose entry names in double quotes</td></tr>\n            <tr><td><span class=\"flag-pill\">-r</span> <span class=\"flag-pill long\">--reverse</span></td><td>Reverse order while sorting</td></tr>\n            <tr><td><span class=\"flag-pill\">-R</span> <span class=\"flag-pill long\">--recursive</span></td><td>List subdirectories recursively</td></tr>\n            <tr><td><span class=\"flag-pill\">-s</span> <span class=\"flag-pill long\">--size</span></td><td>Print the allocated size of each file, in blocks</td></tr>\n            <tr><td><span class=\"flag-pill\">-S</span></td><td>Sort by file size, largest first</td></tr>\n            <tr><td><span class=\"flag-pill\">-t</span></td><td>Sort by modification time, newest first</td></tr>\n            <tr><td><span class=\"flag-pill\">-T</span> <span class=\"flag-pill long\">--tabsize=COLS</span></td><td>Assume tab stops at every COLS instead of 8</td></tr>\n            <tr><td><span class=\"flag-pill\">-u</span></td><td>Sort by/atime; with <code>-l</code> show atime</td></tr>\n            <tr><td><span class=\"flag-pill\">-U</span></td><td>Do not sort; list entries in directory order</td></tr>\n            <tr><td><span class=\"flag-pill\">-v</span></td><td>Natural sort of (version) numbers within text</td></tr>\n            <tr><td><span class=\"flag-pill\">-w</span> <span class=\"flag-pill long\">--width=COLS</span></td><td>Set output width to COLS (0 = no wrap)</td></tr>\n            <tr><td><span class=\"flag-pill\">-x</span></td><td>List entries by lines instead of by columns</td></tr>\n            <tr><td><span class=\"flag-pill\">-X</span></td><td>Sort alphabetically by entry extension</td></tr>\n            <tr><td><span class=\"flag-pill\">-1</span></td><td>List one file per line</td></tr>\n            <tr><td><span class=\"flag-pill long\">--color[=WHEN]</span></td><td>Colorize output: <code>never</code> / <code>auto</code> / <code>always</code></td></tr>\n            <tr><td><span class=\"flag-pill long\">--format=WORD</span></td><td>Format: <code>across</code> / <code>commas</code> / <code>horizontal</code> / <code>long</code> / <code>single-column</code> / <code>verbose</code> / <code>vertical</code></td></tr>\n            <tr><td><span class=\"flag-pill long\">--sort=WORD</span></td><td>Sort by: <code>none</code> / <code>size</code> / <code>time</code> / <code>version</code> / <code>extension</code></td></tr>\n            <tr><td><span class=\"flag-pill long\">--time=WORD</span></td><td>Show time as: <code>atime</code> / <code>access</code> / <code>use</code> / <code>ctime</code> / <code>status</code> / <code>birth</code> / <code>created</code> / <code>mtime</code> / <code>modification</code></td></tr>\n            <tr><td><span class=\"flag-pill long\">--time-style=STYLE</span></td><td><code>full-iso</code>, <code>long-iso</code>, <code>iso</code>, <code>locale</code>, <code>+FORMAT</code> (strftime)</td></tr>\n            <tr><td><span class=\"flag-pill long\">--block-size=SIZE</span></td><td>Scale sizes by SIZE: <code>K</code>, <code>M</code>, <code>G</code>... or bytes</td></tr>\n            <tr><td><span class=\"flag-pill long\">--author</span></td><td>With <code>-l</code>, print the author of each file (GNU)</td></tr>\n            <tr><td><span class=\"flag-pill long\">--group-directories-first</span></td><td>Group directories before files in the listing</td></tr>\n            <tr><td><span class=\"flag-pill long\">--hide=PATTERN</span></td><td>Hide entries matching PATTERN (e.g. <code>--hide='*.o'</code>)</td></tr>\n            <tr><td><span class=\"flag-pill long\">--si</span></td><td>Like <code>-h</code> but powers of 1000 (kB, MB, GB)</td></tr>\n            <tr><td><span class=\"flag-pill long\">--version</span></td><td>Output version and exit</td></tr>\n            <tr><td><span class=\"flag-pill long\">--help</span></td><td>Show help text and exit</td></tr>\n          </tbody>\n        </table>\n      </div>\n    </div>\n  </section>\n\n  <!-- ==================== LONG FORMAT ==================== -->\n  <section id=\"long\">\n    <h2 class=\"ls-heading\"><i class=\"bi bi-list-ol me-2\"></i>Long Format — <code>ls -l</code> Decoded</h2>\n    <p>The long format packs nine pieces of information on every line.</p>\n\n    <div class=\"card\">\n      <div class=\"card-header\">Sample line</div>\n      <div class=\"card-body\">\n<pre class=\"term no-prompt\"><code><span class=\"<span\">\"c-output\"</span>&gt;-rw-r--r--  <span class=\"num\">1</span> alice staff  <span class=\"num\">1.2</span>K  Jun  <span class=\"num\">4</span> <span class=\"num\">09</span>:<span class=\"num\">30</span>  notes.txt</code></pre>\n      </div>\n    </div>\n\n    <div class=\"card\">\n      <div class=\"table-responsive\">\n        <table class=\"table table-hover mb-0\">\n          <thead class=\"table-light\">\n            <tr><th style=\"width:18%\">Field</th><th style=\"width:20%\">Example</th><th>Meaning</th></tr>\n          </thead>\n          <tbody>\n            <tr><td>1. Type</td><td><code>-</code></td><td><code>-</code>=file, <code>d</code>=dir, <code>l</code>=symlink, <code>b</code>=block, <code>c</code>=char, <code>p</code>=FIFO, <code>s</code>=socket</td></tr>\n            <tr><td>2. Permissions</td><td><code>rw-r--r--</code></td><td>owner / group / other — r=4, w=2, x=1</td></tr>\n            <tr><td>3. Hard links</td><td><code>1</code></td><td>Number of hard links to the inode</td></tr>\n            <tr><td>4. Owner</td><td><code>alice</code></td><td>User name (or UID with <code>-n</code>)</td></tr>\n            <tr><td>5. Group</td><td><code>staff</code></td><td>Group name (or GID with <code>-n</code>)</td></tr>\n            <tr><td>6. Size</td><td><code>1.2K</code></td><td>Bytes · use <code>-h</code> for KB/MB/GB</td></tr>\n            <tr><td>7. Month</td><td><code>Jun</code></td><td>Modification month</td></tr>\n            <tr><td>8. Day</td><td><code>4</code></td><td>Modification day of month</td></tr>\n            <tr><td>9. Time</td><td><code>09:30</code></td><td>HH:MM, or year if older than 6 months</td></tr>\n            <tr><td>10. Name</td><td><code>notes.txt</code></td><td>File or directory name</td></tr>\n          </tbody>\n        </table>\n      </div>\n    </div>\n\n    <div class=\"info-box\">\n      <strong>Pro tip:</strong> Use <code>ls -lhF</code> for the \"default\" long view most people want: long, human sizes, classify.\n    </div>\n  </section>\n\n  <!-- ==================== HIDDEN ==================== -->\n  <section id=\"hidden\">\n    <h2 class=\"ls-heading\"><i class=\"bi bi-eye-slash me-2\"></i>Hidden Files</h2>\n    <p>On Unix, any file whose name starts with a dot (<code>.</code>) is hidden. <code>ls</code> skips them by default.</p>\n\n    <div class=\"card\">\n      <div class=\"card-body\">\n<pre class=\"term\"><code><span class=\"c-cmd\">ls -a</span>                  <span class=\"c-comment\"># show all, including . and ..</span>\n<span class=\"c-cmd\">ls -A</span>                  <span class=\"c-comment\"># show all, except . and ..</span>\n<span class=\"c-cmd\">ls -la ~</span>               <span class=\"c-comment\"># long listing of all home-dir entries</span>\n<span class=\"c-cmd\">ls -d .[!.]*</span>          <span class=\"c-comment\"># only hidden files (bash glob)</span>\n<span class=\"c-cmd\">ls --hide='*.log'</span>     <span class=\"c-comment\"># hide by pattern (no leading dot required)</span>\n<span class=\"c-cmd\">ls -I '*.tmp'</span>         <span class=\"c-comment\"># ignore pattern</span></code></pre>\n      </div>\n    </div>\n\n    <div class=\"card\">\n      <div class=\"card-header\">Typical dotfiles</div>\n      <div class=\"card-body\">\n<pre class=\"term no-prompt\"><code><span class=\"<span\">\"c-output\"</span>&gt;.bashrc        <span class=\"cm\"># login shell config\n</span>\n.bash_profile  <span class=\"cm\"># sourced at login\n</span>\n.zshrc         <span class=\"cm\"># zsh config (macOS default since Catalina)\n</span>\n.gitconfig     <span class=\"cm\"># git user/aliases\n</span>\n.<span class=\"kw\">ssh</span>/          <span class=\"cm\"># SSH keys &amp; known_hosts\n</span>\n.vscode/       <span class=\"cm\"># VS Code settings\n</span>\n.ENV / .<span class=\"kw\">env</span>    <span class=\"cm\"># environment variables (never commit!)\n</span>\n.dockerignore  <span class=\"cm\"># files excluded from Docker context</span></code></pre>\n      </div>\n    </div>\n  </section>\n\n  <!-- ==================== SORTING ==================== -->\n  <section id=\"sort\">\n    <h2 class=\"ls-heading\"><i class=\"bi bi-sort-down me-2\"></i>Sorting</h2>\n\n    <div class=\"card\">\n      <div class=\"card-body\">\n<pre class=\"term\"><code><span class=\"c-cmd\">ls</span>                       <span class=\"c-comment\"># alphabetical (default)</span>\n<span class=\"c-cmd\">ls -r</span>                   <span class=\"c-comment\"># reverse alphabetical</span>\n<span class=\"c-cmd\">ls -t</span>                   <span class=\"c-comment\"># newest first (mtime)</span>\n<span class=\"c-cmd\">ls -tr</span>                  <span class=\"c-comment\"># oldest first</span>\n<span class=\"c-cmd\">ls -S</span>                   <span class=\"c-comment\"># largest first</span>\n<span class=\"c-cmd\">ls -Sr</span>                  <span class=\"c-comment\"># smallest first</span>\n<span class=\"c-cmd\">ls -X</span>                   <span class=\"c-comment\"># by extension</span>\n<span class=\"c-cmd\">ls -v</span>                   <span class=\"c-comment\"># natural sort (file1, file2, file10)</span>\n<span class=\"c-cmd\">ls -U</span>                   <span class=\"c-comment\"># unsorted (directory order)</span>\n<span class=\"c-cmd\">ls --sort=time</span>         <span class=\"c-comment\"># same as -t</span>\n<span class=\"c-cmd\">ls -l --time=birth</span>     <span class=\"c-comment\"># sort by creation time (birth)</span>\n<span class=\"c-cmd\">ls -l --time-style=full-iso</span>   <span class=\"c-comment\"># 2026-06-04 09:30:42.000000000 +0530</span>\n<span class=\"c-cmd\">ls -l --time-style=long-iso</span>   <span class=\"c-comment\"># 2026-06-04 09:30</span>\n<span class=\"c-cmd\">ls -l --time-style='+%Y-%m-%d %H:%M'</span>  <span class=\"c-comment\"># strftime format</span></code></pre>\n      </div>\n    </div>\n\n    <div class=\"tip-box\">\n      <strong>Tip:</strong> <code>ls -ltr</code> is the universal \"show me the oldest files at the bottom\" recipe used by sysadmins.\n    </div>\n  </section>\n\n  <!-- ==================== RECURSIVE ==================== -->\n  <section id=\"recursive\">\n    <h2 class=\"ls-heading\"><i class=\"bi bi-diagram-3 me-2\"></i>Recursive Listing</h2>\n\n    <div class=\"card\">\n      <div class=\"card-body\">\n<pre class=\"term\"><code><span class=\"c-cmd\">ls -R</span>                   <span class=\"c-comment\"># recurse into subdirectories</span>\n<span class=\"c-cmd\">ls -R src/</span>             <span class=\"c-comment\"># recurse from a specific path</span>\n<span class=\"c-cmd\">ls -Rl</span>                 <span class=\"c-comment\"># long + recursive</span>\n<span class=\"c-cmd\">ls -lR | less</span>          <span class=\"c-comment\"># scroll long trees with a pager</span>\n<span class=\"c-cmd\">ls -d $(find . -type d)</span>  <span class=\"c-comment\"># list all directories only</span>\n<span class=\"c-cmd\">tree</span>                   <span class=\"c-comment\"># pretty tree (install via: apt install tree / brew install tree)</span>\n<span class=\"c-cmd\">tree -L 2</span>              <span class=\"c-comment\"># limit to 2 levels deep</span>\n<span class=\"c-cmd\">tree -a -I 'node_modules|.git'</span>  <span class=\"c-comment\"># ignore build/output dirs</span></code></pre>\n      </div>\n    </div>\n\n    <div class=\"warn-box\">\n      <strong>Careful:</strong> <code>ls -R /</code> on a system with millions of files (think <code>/var</code>) may take a while. Pipe through <code>less</code> or filter with <code>grep</code>.\n    </div>\n  </section>\n\n  <!-- ==================== FILTER ==================== -->\n  <section id=\"filter\">\n    <h2 class=\"ls-heading\"><i class=\"bi bi-funnel me-2\"></i>Filtering &amp; Glob Patterns</h2>\n    <p>Glob patterns are expanded by the shell, not by <code>ls</code> itself.</p>\n\n    <div class=\"card\">\n      <div class=\"card-body\">\n<pre class=\"term\"><code><span class=\"c-cmd\">ls *.txt</span>                 <span class=\"c-comment\"># all .txt files</span>\n<span class=\"c-cmd\">ls file?.log</span>            <span class=\"c-comment\"># file1.log, fileA.log (any single char)</span>\n<span class=\"c-cmd\">ls file[0-9].log</span>        <span class=\"c-comment\"># digits 0-9 only</span>\n<span class=\"c-cmd\">ls [A-Z]*.md</span>            <span class=\"c-comment\"># uppercase initial letter</span>\n<span class=\"c-cmd\">ls **/*.js</span>              <span class=\"c-comment\"># recursive (needs: shopt -s globstar in bash)</span>\n<span class=\"c-cmd\">ls -I '*.min.js'</span>         <span class=\"c-comment\"># ignore minified files</span>\n<span class=\"c-cmd\">ls -d */</span>                <span class=\"c-comment\"># only directories</span>\n<span class=\"c-cmd\">ls -F | grep -v '/$'</span>    <span class=\"c-comment\"># only files (non-dirs)</span>\n<span class=\"c-cmd\">ls -1 | wc -l</span>           <span class=\"c-comment\"># count entries</span>\n<span class=\"c-cmd\">ls -lR | grep '^d'</span>      <span class=\"c-comment\"># only directories (long format)</span></code></pre>\n      </div>\n    </div>\n\n    <div class=\"card\">\n      <div class=\"card-header\">Glob cheat sheet</div>\n      <div class=\"card-body\">\n        <div class=\"table-responsive\">\n          <table class=\"table table-sm cmd-table mb-0\">\n            <thead class=\"table-light\"><tr><th>Pattern</th><th>Matches</th></tr></thead>\n            <tbody>\n              <tr><td><code>*</code></td><td>Any string (no slash)</td></tr>\n              <tr><td><code>?</code></td><td>Any single char</td></tr>\n              <tr><td><code>[abc]</code></td><td>Any one of a, b, c</td></tr>\n              <tr><td><code>[a-z]</code></td><td>Any char in range a–z</td></tr>\n              <tr><td><code>[!abc]</code></td><td>Any char except a, b, c</td></tr>\n              <tr><td><code>{a,b,c}</code></td><td>Brace expansion (e.g. <code>file.{js,ts}</code>)</td></tr>\n            </tbody>\n          </table>\n        </div>\n      </div>\n    </div>\n  </section>\n\n  <!-- ==================== CMD ==================== -->\n  <section id=\"cmd\">\n    <h2 class=\"cmd-heading\"><i class=\"bi bi-windows me-2\"></i>Windows CMD Equivalent — <code>dir</code></h2>\n    <p>The closest built-in equivalent of <code>ls</code> in Windows Command Prompt is <code>dir</code>. It produces similar information but with different flags and formatting.</p>\n\n    <div class=\"card\">\n      <div class=\"card-header\">Side-by-side — <code>ls</code> ↔ <code>dir</code></div>\n      <div class=\"card-body\">\n        <div class=\"table-responsive\">\n          <table class=\"table table-hover mb-0 cmd-table\">\n            <thead class=\"table-light\">\n              <tr><th style=\"width:36%\">ls (Linux/macOS)</th><th>dir (Windows CMD)</th><th>What it does</th></tr>\n            </thead>\n            <tbody>\n              <tr><td><code>ls</code></td><td><code>dir</code></td><td>List current directory</td></tr>\n              <tr><td><code>ls -l</code></td><td><code>dir</code></td><td>Long listing (dir is long by default)</td></tr>\n              <tr><td><code>ls -a</code></td><td><code>dir /a</code></td><td>Show hidden + system files</td></tr>\n              <tr><td><code>ls -A</code></td><td><code>dir /a</code></td><td>Show all but <code>.</code> / <code>..</code></td></tr>\n              <tr><td><code>ls -la</code></td><td><code>dir /a</code></td><td>Long + hidden</td></tr>\n              <tr><td><code>ls -lh</code></td><td><code>dir</code></td><td>Long (already shows commas)</td></tr>\n              <tr><td><code>ls -R</code></td><td><code>dir /s</code></td><td>Recursive into subdirs</td></tr>\n              <tr><td><code>ls -1</code></td><td><code>dir /b</code></td><td>Bare format, one per line</td></tr>\n              <tr><td><code>ls -d */</code></td><td><code>dir /ad</code></td><td>Directories only</td></tr>\n              <tr><td><code>ls -t</code></td><td><code>dir /od</code></td><td>Sorted by date (oldest first)</td></tr>\n              <tr><td><code>ls -tr</code></td><td><code>dir /o-d</code></td><td>Newest first</td></tr>\n              <tr><td><code>ls -S</code></td><td><code>dir /os</code></td><td>Sorted by size (smallest first)</td></tr>\n              <tr><td><code>ls -Sr</code></td><td><code>dir /o-s</code></td><td>Largest first</td></tr>\n              <tr><td><code>ls -X</code></td><td><code>dir /oe</code></td><td>Sorted by extension</td></tr>\n              <tr><td><code>ls -r</code></td><td><code>dir /o-n</code></td><td>Reverse alpha order</td></tr>\n              <tr><td><code>ls *.txt</code></td><td><code>dir *.txt</code></td><td>Filter by glob</td></tr>\n              <tr><td><code>ls /path/to/dir</code></td><td><code>dir C:\\path\\to\\dir</code></td><td>Absolute path</td></tr>\n              <tr><td><code>ls ..</code></td><td><code>dir ..</code></td><td>Parent directory</td></tr>\n              <tr><td><code>ls -i</code></td><td><code>dir /q</code></td><td>(different: shows owner, not inode)</td></tr>\n              <tr><td><code>ls -n</code></td><td><code>dir /-c</code></td><td>No thousands separator in sizes</td></tr>\n              <tr><td><code>ls -F</code></td><td><code>dir /-d</code></td><td>(different: dir itself has no <code>-F</code>)</td></tr>\n              <tr><td><code>ls -1 | wc -l</code></td><td><code>dir | find /c \"file\"</code></td><td>Count files</td></tr>\n            </tbody>\n          </table>\n        </div>\n      </div>\n    </div>\n\n    <div class=\"card\">\n      <div class=\"card-header\">Useful <code>dir</code> flags (no <code>ls</code> equivalent)</div>\n      <div class=\"card-body\">\n        <div class=\"table-responsive\">\n          <table class=\"table table-hover cmd-table mb-0\">\n            <thead class=\"table-light\"><tr><th style=\"width:34%\">Flag</th><th>Description</th></tr></thead>\n            <tbody>\n              <tr><td><code>/p</code></td><td>Pause after each screen of information</td></tr>\n              <tr><td><code>/w</code></td><td>Wide list format (sort by columns)</td></tr>\n              <tr><td><code>/x</code></td><td>Show 8.3 short names for non-8dot3 files</td></tr>\n              <tr><td><code>/c</code></td><td>Show thousand separator in file sizes (default)</td></tr>\n              <tr><td><code>/-c</code></td><td>Disable thousand separator</td></tr>\n              <tr><td><code>/q</code></td><td>Show file owner</td></tr>\n              <tr><td><code>/a[:attributes]</code></td><td>Show files with attribute: <code>d</code> dirs, <code>h</code> hidden, <code>s</code> system, <code>r</code> read-only, <code>a</code> archive</td></tr>\n              <tr><td><code>/o[:order]</code></td><td>Sort by: <code>n</code> name, <code>s</code> size, <code>e</code> extension, <code>d</code> date, <code>g</code> group</td></tr>\n              <tr><td><code>/t[:timefield]</code></td><td>Time field: <code>c</code> creation, <code>a</code> last access, <code>w</code> last written (default)</td></tr>\n            </tbody>\n          </table>\n        </div>\n      </div>\n    </div>\n\n    <div class=\"card\">\n      <div class=\"card-header\">Sample <code>dir</code> output</div>\n      <div class=\"card-body\">\n<pre class=\"term no-prompt\"><code><span class=\"c-cmdwin\">C:\\Users\\Alice&gt;</span> <span class=\"c-ps\">dir</span> <span class=\"c-output\">C:\\Windows\n Volume in drive C has no label.\n Volume Serial Number is <span class=\"num\">1234</span>-<span class=\"num\">5678</span>\n\n Directory of C:\\Windows\n\n<span class=\"num\">06</span>/<span class=\"num\">04</span>/<span class=\"num\">2026</span>  <span class=\"num\">09</span>:<span class=\"num\">30</span> AM    <span class=\"kw\">&lt;DIR</span><span class=\"kw\">&gt;</span>          .\n<span class=\"num\">06</span>/<span class=\"num\">04</span>/<span class=\"num\">2026</span>  <span class=\"num\">09</span>:<span class=\"num\">30</span> AM    <span class=\"kw\">&lt;DIR</span><span class=\"kw\">&gt;</span>          ..\n<span class=\"num\">06</span>/<span class=\"num\">03</span>/<span class=\"num\">2026</span>  <span class=\"num\">08</span>:<span class=\"num\">15</span> PM             <span class=\"num\">1</span>,<span class=\"num\">234</span> addins\n<span class=\"num\">06</span>/<span class=\"num\">02</span>/<span class=\"num\">2026</span>  <span class=\"num\">10</span>:<span class=\"num\">42</span> AM    <span class=\"kw\">&lt;DIR</span><span class=\"kw\">&gt;</span>          AppReadiness\n<span class=\"num\">05</span>/<span class=\"num\">29</span>/<span class=\"num\">2026</span>  <span class=\"num\">02</span>:<span class=\"num\">11</span> PM           <span class=\"num\">123</span>,<span class=\"num\">456</span> explorer.exe\n              <span class=\"num\">15</span> File(s)      <span class=\"num\">4</span>,<span class=\"num\">567</span>,<span class=\"num\">890</span> bytes\n              <span class=\"num\">22</span> Dir(s)  <span class=\"num\">123</span>,<span class=\"num\">456</span>,<span class=\"num\">789</span>,<span class=\"num\">012</span> bytes free</span></code></pre>\n      </div>\n    </div>\n\n    <div class=\"tip-box\">\n      <strong>Tip:</strong> If you prefer Unix-style on Windows, install <strong>Git for Windows</strong> to get <code>ls</code>, <code>grep</code>, <code>awk</code>, and a real Bash terminal — free and ~50 MB.\n    </div>\n  </section>\n\n  <!-- ==================== POWERSHELL ==================== -->\n  <section id=\"powershell\">\n    <h2 class=\"ps-heading\"><i class=\"bi bi-lightning me-2\"></i>PowerShell Equivalent</h2>\n    <p>PowerShell on Windows includes <code>ls</code> and <code>dir</code> as built-in <strong>aliases</strong> for the <code>Get-ChildItem</code> cmdlet.\n       For richer output, use <code>Get-ChildItem</code> directly.</p>\n\n    <div class=\"card\">\n      <div class=\"card-body\">\n<pre class=\"term\"><code><span class=\"c-ps\">ls</span>                              <span class=\"c-comment\"># alias for Get-ChildItem</span>\n<span class=\"c-ps\">dir</span>                             <span class=\"c-comment\"># also alias for Get-ChildItem</span>\n<span class=\"c-ps\">gci</span>                             <span class=\"c-comment\"># shorter alias</span>\n<span class=\"c-ps\">Get-ChildItem</span>                  <span class=\"c-comment\"># full cmdlet name</span>\n<span class=\"c-ps\">Get-ChildItem -Force</span>           <span class=\"c-comment\"># like ls -a (include hidden)</span>\n<span class=\"c-ps\">Get-ChildItem -Recurse</span>        <span class=\"c-comment\"># like ls -R</span>\n<span class=\"c-ps\">Get-ChildItem -File</span>           <span class=\"c-comment\"># files only</span>\n<span class=\"c-ps\">Get-ChildItem -Directory</span>      <span class=\"c-comment\"># directories only (like ls -d */)</span>\n<span class=\"c-ps\">Get-ChildItem *.txt</span>           <span class=\"c-comment\"># filter by glob</span>\n<span class=\"c-ps\">ls | Sort-Object Length -Descending</span>  <span class=\"c-comment\"># like ls -S</span>\n<span class=\"c-ps\">ls | Sort-Object LastWriteTime -Descending</span>  <span class=\"c-comment\"># like ls -t</span>\n<span class=\"c-ps\">Get-ChildItem | Select Name,Length,LastWriteTime</span>  <span class=\"c-comment\"># custom columns</span>\n<span class=\"c-ps\">ls | Where-Object { $_.Length -gt 1MB }</span>  <span class=\"c-comment\"># filter by size</span>\n<span class=\"c-ps\">(Get-ChildItem).Count</span>        <span class=\"c-comment\"># count files (like ls -1 | wc -l)</span>\n<span class=\"c-ps\">tree /F</span>                        <span class=\"c-comment\"># pretty tree (built-in)</span>\n<span class=\"c-ps\">tree /F /A</span>                     <span class=\"c-comment\"># tree with ASCII only (no Unicode lines)</span></code></pre>\n      </div>\n    </div>\n\n    <div class=\"info-box\">\n      <strong>PowerShell objects &gt; text:</strong> <code>ls</code> in PowerShell returns <strong>objects</strong> with properties (<code>Name</code>, <code>Length</code>, <code>LastWriteTime</code>, <code>Mode</code>), not raw text. Pipe to <code>Format-Table</code>, <code>Where-Object</code>, <code>Select-Object</code> for powerful filtering — something the Unix <code>ls</code> cannot do on its own.\n    </div>\n\n    <div class=\"card\">\n      <div class=\"card-header\">PowerShell: list the 5 largest files in a directory</div>\n      <div class=\"card-body\">\n<pre class=\"term\"><code><span class=\"c-ps\">Get-ChildItem -File -Recurse |\n  Sort-Object Length -Descending |\n  Select-Object -First 5 FullName, @{N='SizeMB';E={[math]::Round($_.Length/1MB,2)}}\n\n<span class=\"c-output\">FullName                                   SizeMB\n--------                                   ------\nC:\\data\\big1.bin                          1024.50\nC:\\data\\big2.zip                            512.25\nC:\\data\\media\\movie.mkv                    256.10\nC:\\data\\backup.tar                          128.00\nC:\\data\\dump.sql                             64.00</span></span></code></pre>\n      </div>\n    </div>\n  </section>\n\n  <!-- ==================== WSL ==================== -->\n  <section id=\"wsl\">\n    <h2 class=\"ls-heading\"><i class=\"bi bi-box me-2\"></i>WSL / Git Bash / MSYS</h2>\n    <p>Three great ways to get real <code>ls</code> on Windows without leaving your machine:</p>\n\n    <div class=\"row g-3\">\n      <div class=\"col-md-4\">\n        <div class=\"card h-100\">\n          <div class=\"card-body\">\n            <h5><i class=\"bi bi-ubuntu text-warning me-2\"></i>WSL</h5>\n            <p class=\"small text-muted mb-2\">Windows Subsystem for Linux. Full Linux <code>ls</code> with all flags.</p>\n<pre class=\"term no-prompt\"><code><span class=\"<span\">\"c-ps\"</span>&gt;wsl <span class=\"kw\">ls</span> <span class=\"fn\">-la</span>\n<span class=\"<span\">\"c-ps\"</span>&gt;wsl <span class=\"kw\">ls</span> /mnt/c/Users</code></pre>\n            <p class=\"small text-muted mb-0\">Access Windows files under <code>/mnt/c/...</code></p>\n          </div>\n        </div>\n      </div>\n      <div class=\"col-md-4\">\n        <div class=\"card h-100\">\n          <div class=\"card-body\">\n            <h5><i class=\"bi bi-git text-danger me-2\"></i>Git Bash</h5>\n            <p class=\"small text-muted mb-2\">Bundled with Git for Windows. Includes coreutils.</p>\n<pre class=\"term no-prompt\"><code><span class=\"<span\">\"c-ps\"</span>&gt;<span class=\"kw\">ls</span> <span class=\"fn\">-lah</span>\n<span class=\"<span\">\"c-ps\"</span>&gt;<span class=\"kw\">ls</span> <span class=\"fn\">-R</span> <span class=\"fn\">--color</span>=auto</code></pre>\n            <p class=\"small text-muted mb-0\">Use the <code>Git Bash Here</code> context-menu.</p>\n          </div>\n        </div>\n      </div>\n      <div class=\"col-md-4\">\n        <div class=\"card h-100\">\n          <div class=\"card-body\">\n            <h5><i class=\"bi bi-terminal text-info me-2\"></i>MSYS2 / Cygwin</h5>\n            <p class=\"small text-muted mb-2\">POSIX compatibility layer. Full Unix toolchain.</p>\n<pre class=\"term no-prompt\"><code><span class=\"<span\">\"c-ps\"</span>&gt;<span class=\"kw\">pacman</span> <span class=\"fn\">-S</span> coreutils\n<span class=\"<span\">\"c-ps\"</span>&gt;<span class=\"kw\">ls</span> <span class=\"fn\">--color</span>=auto</code></pre>\n            <p class=\"small text-muted mb-0\">Heavier than Git Bash, more complete than WSL.</p>\n          </div>\n        </div>\n      </div>\n    </div>\n  </section>\n\n  <!-- ==================== RECIPES ==================== -->\n  <section id=\"recipes\">\n    <h2 class=\"recipes-heading\"><i class=\"bi bi-bookmark-star me-2\"></i>Recipes — Real-World Patterns</h2>\n\n    <div class=\"card\">\n      <div class=\"card-header\">Find the 10 largest files anywhere under a path</div>\n      <div class=\"card-body\">\n<pre class=\"term\"><code><span class=\"c-cmd\">find /var -type f -exec ls -lh {} +</span> 2&gt;/dev/null \\\n  | <span class=\"c-cmd\">sort -k5 -h -r</span> \\\n  | <span class=\"c-cmd\">head -n 10</span>\n\n<span class=\"c-comment\"># One-liner (no find needed on most distros)\n</span><span class=\"c-cmd\">ls -lAR /var</span> | <span class=\"c-cmd\">sort -k5 -h -r</span> | <span class=\"c-cmd\">head</span></code></pre>\n      </div>\n    </div>\n\n    <div class=\"card\">\n      <div class=\"card-header\">List every file modified in the last 24 hours</div>\n      <div class=\"card-body\">\n<pre class=\"term\"><code><span class=\"c-cmd\">ls -lt --time-style=full-iso</span> \\\n  | <span class=\"c-cmd\">awk '$6 ~ /^'\"$(date -u +%Y-%m-%d)\"'/'</span>\n\n<span class=\"c-comment\"># Better: combine with find for accuracy\n</span><span class=\"c-cmd\">find . -type f -mtime -1 -exec ls -lh {} +</span></code></pre>\n      </div>\n    </div>\n\n    <div class=\"card\">\n      <div class=\"card-header\">Count files by extension</div>\n      <div class=\"card-body\">\n<pre class=\"term\"><code><span class=\"c-cmd\">ls -1 *.js 2&gt;/dev/null | wc -l</span>\n<span class=\"c-cmd\">ls -1 | sed 's/.*\\.//' | sort | uniq -c | sort -rn</span>\n\n<span class=\"c-output\">   42 js\n   18 ts\n   12 md\n    5 json</span></code></pre>\n      </div>\n    </div>\n\n    <div class=\"card\">\n      <div class=\"card-header\">List files with absolute paths</div>\n      <div class=\"card-body\">\n<pre class=\"term\"><code><span class=\"c-cmd\">ls -d \"$PWD\"/*</span>                       <span class=\"c-comment\"># current dir</span>\n<span class=\"c-cmd\">ls -d \"$PWD\"/**/*(.)</span>                <span class=\"c-comment\"># zsh globstar, files only</span>\n<span class=\"c-cmd\">find \"$(pwd)\" -maxdepth 1 -mindepth 1</span>  <span class=\"c-comment\"># portable</span></code></pre>\n      </div>\n    </div>\n\n    <div class=\"card\">\n      <div class=\"card-header\">Watch a directory in real time (refresh every 2s)</div>\n      <div class=\"card-body\">\n<pre class=\"term\"><code><span class=\"c-cmd\">watch -n 2 'ls -lh --color=auto'</span>           <span class=\"c-comment\"># Linux</span>\n<span class=\"c-cmd\">while true; do clear; ls -lh; sleep 2; done</span>   <span class=\"c-comment\"># portable bash</span></code></pre>\n      </div>\n    </div>\n\n    <div class=\"card\">\n      <div class=\"card-header\">Compare two directories (file names only)</div>\n      <div class=\"card-body\">\n<pre class=\"term\"><code><span class=\"c-cmd\">diff &lt;(ls -1 /etc) &lt;(ls -1 /etc.old)</span>\n\n<span class=\"c-comment\"># Compare via contents\n</span><span class=\"c-cmd\">diff -rq /etc /etc.old</span></code></pre>\n      </div>\n    </div>\n\n    <div class=\"card\">\n      <div class=\"card-header\">Find duplicate filenames across subdirectories</div>\n      <div class=\"card-body\">\n<pre class=\"term\"><code><span class=\"c-cmd\">ls -R | awk '/:$/ {dir=$0; next} NF {print dir\"/\"$0}'</span> \\\n  | <span class=\"c-cmd\">awk -F/ '{print $NF}'</span> \\\n  | <span class=\"c-cmd\">sort | uniq -d</span></code></pre>\n      </div>\n    </div>\n\n    <div class=\"card\">\n      <div class=\"card-header\">Show inode usage (find hard-linked duplicates)</div>\n      <div class=\"card-body\">\n<pre class=\"term\"><code><span class=\"c-cmd\">ls -li</span> | <span class=\"c-cmd\">sort -n</span>            <span class=\"c-comment\"># inodes ascending → duplicates cluster</span>\n<span class=\"c-cmd\">find . -inum 1234567</span>       <span class=\"c-comment\"># show all hard links to inode</span></code></pre>\n      </div>\n    </div>\n\n    <div class=\"card\">\n      <div class=\"card-header\">Total size of current directory</div>\n      <div class=\"card-body\">\n<pre class=\"term\"><code><span class=\"c-cmd\">ls -lAR | awk '/^total/ {sum += $1} END {print sum, \"blocks\"}'</span>\n<span class=\"c-cmd\">du -sh .</span>      <span class=\"c-comment\"># the more common way</span></code></pre>\n      </div>\n    </div>\n  </section>\n\n  <!-- ==================== TIPS ==================== -->\n  <section id=\"tips\">\n    <h2 class=\"tips-heading\"><i class=\"bi bi-lightbulb me-2\"></i>Pro Tips &amp; Tricks</h2>\n\n    <div class=\"row g-3\">\n      <div class=\"col-md-6\">\n        <div class=\"tip-box h-100\">\n          <strong>1. The “show me everything” alias</strong>\n<pre class=\"term\"><code><span class=\"c-cmd\">alias ll='ls -lahF'</span>\n<span class=\"c-cmd\">alias la='ls -A'</span>\n<span class=\"c-cmd\">alias l='ls -CF'</span>\n\n<span class=\"c-comment\"># Make it permanent (bash)\n</span><span class=\"c-cmd\">echo \"alias ll='ls -lahF'\" &gt;&gt; ~/.bashrc</span>\n<span class=\"c-cmd\">source ~/.bashrc</span></code></pre>\n        </div>\n      </div>\n      <div class=\"col-md-6\">\n        <div class=\"tip-box h-100\">\n          <strong>2. Colorize even when piped (force it on)</strong>\n<pre class=\"term\"><code><span class=\"c-cmd\">ls --color=always | less -R</span>\n<span class=\"c-cmd\">ls -G | grep --color=never pattern</span>   <span class=\"c-comment\"># grep on macOS, then strip</span></code></pre>\n        </div>\n      </div>\n      <div class=\"col-md-6\">\n        <div class=\"tip-box h-100\">\n          <strong>3. List only directories</strong>\n<pre class=\"term\"><code><span class=\"c-cmd\">ls -d */                      </span><span class=\"c-comment\"># trailing slash trick</span>\n<span class=\"c-cmd\">ls -l | grep '^d'             </span><span class=\"c-comment\"># long format filter</span>\n<span class=\"c-cmd\">ls -d .*/ */ 2&gt;/dev/null      </span><span class=\"c-comment\"># include hidden dirs</span>\n<span class=\"c-cmd\">find . -maxdepth 1 -type d    </span><span class=\"c-comment\"># portable</span></code></pre>\n        </div>\n      </div>\n      <div class=\"col-md-6\">\n        <div class=\"tip-box h-100\">\n          <strong>4. ISO timestamps in long listing</strong>\n<pre class=\"term\"><code><span class=\"c-cmd\">ls -l --time-style=long-iso</span>\n<span class=\"c-output\">-rw-r--r-- 1 alice staff 1200 2026-06-04 09:30 notes.txt</span>\n\n<span class=\"c-cmd\">ls -l --time-style=full-iso</span>\n<span class=\"c-output\">-rw-r--r-- 1 alice staff 1200 2026-06-04 09:30:42.123456789 +0530 notes.txt</span></code></pre>\n        </div>\n      </div>\n      <div class=\"col-md-6\">\n        <div class=\"tip-box h-100\">\n          <strong>5. Numeric permissions (chmod-style)</strong>\n<pre class=\"term\"><code><span class=\"c-cmd\">ls -l | awk '{print $1}' | tail -n +2</span>\n\n<span class=\"c-cmd\"># rwxr-xr-x → 4+2+1  4+0+1  4+0+1 = 755\n# rw-r--r-- → 4+2+0  4+0+0  4+0+0 = 644\n# rwxrwxrwx → 777\n</span><span class=\"c-cmd\">stat -c '%a %n' *</span>             <span class=\"c-comment\"># better tool</span></code></pre>\n        </div>\n      </div>\n      <div class=\"col-md-6\">\n        <div class=\"tip-box h-100\">\n          <strong>6. Group directories first</strong>\n<pre class=\"term\"><code><span class=\"c-cmd\">ls --group-directories-first</span>\n<span class=\"c-cmd\">ls -l --group-directories-first -F</span>\n\n<span class=\"c-cmd\"># Make permanent\n</span><span class=\"c-cmd\">alias ls='ls --group-directories-first --color=auto'</span></code></pre>\n        </div>\n      </div>\n      <div class=\"col-md-6\">\n        <div class=\"tip-box h-100\">\n          <strong>7. Avoid the “Total N” header (use &lt;kbd&gt;G&lt;/kbd&gt; in pager)</strong>\n<pre class=\"term\"><code><span class=\"c-cmd\">ls -lh | tail -n +2</span>            <span class=\"c-comment\"># skip the \"total\" line</span>\n\n<span class=\"c-comment\"># Or pipe to less and press G\n</span><span class=\"c-cmd\">ls -lh | less</span>   <span class=\"c-cmd\">$</span> <span class=\"c-output\">then: press &lt;G&gt; to go to bottom</span></code></pre>\n        </div>\n      </div>\n      <div class=\"col-md-6\">\n        <div class=\"tip-box h-100\">\n          <strong>8. Quote names with spaces in scripts</strong>\n<pre class=\"term\"><code><span class=\"c-cmd\">ls -Q</span>                           <span class=\"c-comment\"># double-quote names</span>\n<span class=\"c-cmd\">ls -1 | while read -r f; do\n  echo \"File: $f\"\ndone</span>\n\n<span class=\"c-cmd\">find . -maxdepth 1 -print0 | xargs -0 -I{} echo {}</span>   <span class=\"c-comment\"># null-delimited (safest)</span></code></pre>\n        </div>\n      </div>\n      <div class=\"col-md-6\">\n        <div class=\"tip-box h-100\">\n          <strong>9. Block-size overrides</strong>\n<pre class=\"term\"><code><span class=\"c-cmd\">ls -l --block-size=K</span>     <span class=\"c-comment\"># always show KB</span>\n<span class=\"c-cmd\">ls -l --block-size=M</span>     <span class=\"c-comment\"># always show MB</span>\n<span class=\"c-cmd\">ls -lh --si</span>             <span class=\"c-comment\"># powers of 1000 (kB, MB, GB)</span></code></pre>\n        </div>\n      </div>\n      <div class=\"col-md-6\">\n        <div class=\"tip-box h-100\">\n          <strong>10. Production-safe defaults (for scripts)</strong>\n<pre class=\"term\"><code><span class=\"c-cmd\">ls -1 --color=never</span>          <span class=\"c-comment\"># one per line, no escape codes</span>\n<span class=\"c-cmd\">LC_ALL=C ls -1</span>              <span class=\"c-comment\"># locale-independent sort</span>\n<span class=\"c-cmd\">ls --quoting-style=literal</span>  <span class=\"c-comment\"># no quotes, no escapes</span></code></pre>\n        </div>\n      </div>\n    </div>\n  </section>\n\n  <!-- ==================== ERRORS ==================== -->\n  <section id=\"errors\">\n    <h2 class=\"ls-heading\"><i class=\"bi bi-exclamation-triangle me-2\"></i>Common Errors &amp; Fixes</h2>\n\n    <div class=\"card\">\n      <div class=\"table-responsive\">\n        <table class=\"table table-hover mb-0\">\n          <thead class=\"table-light\">\n            <tr><th style=\"width:38%\">Error message</th><th>Cause &amp; fix</th></tr>\n          </thead>\n          <tbody>\n            <tr>\n              <td><code>ls: cannot access 'foo': No such file or directory</code></td>\n              <td>Path doesn't exist. Check spelling or use tab-completion. The directory may have been moved or deleted.</td>\n            </tr>\n            <tr>\n              <td><code>ls: cannot open directory 'foo': Permission denied</code></td>\n              <td>You lack read (r) permission. Fix with <code>sudo chmod +r foo</code> or run as a user with access.</td>\n            </tr>\n            <tr>\n              <td><code>ls: invalid option -- 'X'</code></td>\n              <td>You're probably on BSD/macOS <code>ls</code> where some GNU flags differ. Run <code>man ls</code> for the local man page.</td>\n            </tr>\n            <tr>\n              <td>Coloured output corrupts in pipe</td>\n              <td>ANSI codes are leaking. Use <code>ls --color=never</code> or pipe through <code>sed 's/\\x1b\\[[0-9;]*m//g'</code>.</td>\n            </tr>\n            <tr>\n              <td>Spaces in filenames break <code>for f in $(ls)</code></td>\n              <td>Never use <code>for f in $(ls)</code>. Use <code>find . -maxdepth 1 -print0 | while IFS= read -r -d '' f; do ... done</code> or <code>while read f; do ... done &lt; &lt;(ls -1)</code>.</td>\n            </tr>\n            <tr>\n              <td>Newlines in filenames break scripts</td>\n              <td>Use <code>find -print0</code> + <code>xargs -0</code> or <code>while read -d ''</code>. <code>ls</code> itself handles them, but downstream tools may not.</td>\n            </tr>\n            <tr>\n              <td><code>ls: .: Operation not permitted</code> (macOS)</td>\n              <td>macOS Catalina+ enforces Path Randomization in some apps. <code>ls</code> itself is unaffected — check whether you're inside a sandboxed app's data container.</td>\n            </tr>\n            <tr>\n              <td>Symbolic link loops</td>\n              <td><code>ls -L</code> follows links, can infinite-loop. Use <code>ls -l</code> (don't follow) and look at the <code>-&gt; target</code> arrow.</td>\n            </tr>\n            <tr>\n              <td>Sorting different on Linux vs macOS</td>\n              <td>macOS uses BSD <code>ls</code> which sorts case-insensitively by default. Force POSIX sort: <code>LC_ALL=C ls</code>.</td>\n            </tr>\n          </tbody>\n        </table>\n      </div>\n    </div>\n  </section>\n\n  <!-- ==================== CHEAT SHEET ==================== -->\n  <section id=\"cheatsheet\">\n    <h2 class=\"ls-heading\"><i class=\"bi bi-clipboard-check me-2\"></i>One-Page Cheat Sheet</h2>\n    <div class=\"card\">\n      <div class=\"card-body\">\n<pre class=\"term no-prompt\"><code><span class=\"<span\">\"c-output\"</span>&gt;# == Most common ==\n<span class=\"kw\">ls</span>                 plain\n<span class=\"kw\">ls</span> <span class=\"fn\">-l</span>              long\n<span class=\"kw\">ls</span> <span class=\"fn\">-a</span>              include hidden\n<span class=\"kw\">ls</span> <span class=\"fn\">-la</span>             long + hidden\n<span class=\"kw\">ls</span> <span class=\"fn\">-lh</span>             long + human sizes\n<span class=\"kw\">ls</span> <span class=\"fn\">-lhF</span>            long + human + classify\n<span class=\"kw\">ls</span> <span class=\"fn\">-ltr</span>            long, oldest first (mtime)\n<span class=\"kw\">ls</span> <span class=\"fn\">-lS</span>             long, largest first\n<span class=\"kw\">ls</span> <span class=\"fn\">-R</span>              recursive\n<span class=\"kw\">ls</span> <span class=\"fn\">-1</span>              one per line\n\n<span class=\"cm\"># == Filtering ==</span>\n<span class=\"kw\">ls</span> *.txt           glob (shell expands)\n<span class=\"kw\">ls</span> <span class=\"fn\">-I</span> <span class=\"str\">'*.log'</span>      ignore pattern\n<span class=\"kw\">ls</span> <span class=\"fn\">-d</span> */           only directories\n<span class=\"kw\">ls</span> <span class=\"fn\">-F</span> | <span class=\"kw\">grep</span> <span class=\"fn\">-v</span> <span class=\"str\">'/$'</span>    only files\n<span class=\"kw\">ls</span> <span class=\"fn\">-1</span> | <span class=\"kw\">wc</span> <span class=\"fn\">-l</span>      count\n\n<span class=\"cm\"># == Sorting ==</span>\n<span class=\"kw\">ls</span> <span class=\"fn\">-t</span>              newest first\n<span class=\"kw\">ls</span> <span class=\"fn\">-tr</span>             oldest first\n<span class=\"kw\">ls</span> <span class=\"fn\">-S</span>              largest first\n<span class=\"kw\">ls</span> <span class=\"fn\">-Sr</span>             smallest first\n<span class=\"kw\">ls</span> <span class=\"fn\">-v</span>              natural <span class=\"kw\">sort</span>\n<span class=\"kw\">ls</span> <span class=\"fn\">--sort</span>=<span class=\"kw\">time</span>     explicit\n\n<span class=\"cm\"># == Time ==</span>\n<span class=\"kw\">ls</span> <span class=\"fn\">-l</span> <span class=\"fn\">--time-style</span>=full-iso\n<span class=\"kw\">ls</span> <span class=\"fn\">-l</span> <span class=\"fn\">--time-style</span>=long-iso\n<span class=\"kw\">ls</span> <span class=\"fn\">-l</span> <span class=\"fn\">--time</span>=birth    creation <span class=\"kw\">time</span>\n\n<span class=\"cm\"># == Meta ==</span>\n<span class=\"kw\">ls</span> <span class=\"fn\">-i</span>              inode\n<span class=\"kw\">ls</span> <span class=\"fn\">-n</span>              numeric UIDs/GIDs\n<span class=\"kw\">ls</span> <span class=\"fn\">-g</span>              skip owner\n<span class=\"kw\">ls</span> <span class=\"fn\">-G</span>              skip group\n<span class=\"kw\">ls</span> <span class=\"fn\">-o</span>              skip group\n<span class=\"kw\">ls</span> <span class=\"fn\">-Q</span>              quote names\n<span class=\"kw\">ls</span> <span class=\"fn\">--version</span>       version\n<span class=\"kw\">ls</span> <span class=\"fn\">--help</span>          help\n\n<span class=\"cm\"># == Windows (CMD) ==</span>\ndir                 basic\ndir /a              show all (incl. hidden)\ndir /s              recursive\ndir /b              bare format (one per line)\ndir /od             <span class=\"kw\">sort</span> by <span class=\"kw\">date</span>\ndir /o-d            <span class=\"kw\">sort</span> by <span class=\"kw\">date</span>, reverse\ndir /os             <span class=\"kw\">sort</span> by size\ndir /oe             <span class=\"kw\">sort</span> by extension\ndir /ad             directories only\n\n<span class=\"cm\"># == PowerShell ==</span>\n<span class=\"kw\">ls</span>                  <span class=\"kw\">alias</span> <span class=\"kw\">for</span> Get-ChildItem\nGet-ChildItem <span class=\"fn\">-Force</span> <span class=\"fn\">-Recurse</span> <span class=\"fn\">-File</span>\nGet-ChildItem | Sort Length <span class=\"fn\">-Descending</span> | Select <span class=\"fn\">-First</span> <span class=\"num\">10</span>\n<span class=\"kw\">tree</span> /F /A          pretty <span class=\"kw\">tree</span></code></pre>\n      </div>\n    </div>\n  </section>\n\n  <p class=\"text-muted small mt-5\">\n    <i class=\"bi bi-info-circle\"></i>\n    Pages deployed as-is via GitHub Pages — <a href=\"#/\">back to hub</a>.\n  </p>\n"}